# 趋势跟踪策略 1.0 - 研发与回测设计方案

## 1. 项目目标与趋势跟踪策略1.0

本方案旨在构建一个**趋势跟踪策略研发框架**，分阶段完成策略的设计、回测、调优与稳健性分析，最终服务于 $1000万美元以上量级的主动管理基金。

### 1.1 项目目标

- **标的范围**：全量 ETF + 美股个股（市值 > 20 亿美元，日均成交额 > $20M，标的价格 > $10）
  - **个股**：必须包含已退市股票，防止幸存者偏差。必须考虑公司行为，比如拆股/分红/合并，必须使用adjusted price（复权价格）来做历史回测
  - **ETF**：每个回测时间点只纳入**该时间点之前已发行**的 ETF（按 inception date 过滤）；若不处理，2004年回测会错误地买入2015年才发行的ETF，产生 look-ahead bias
- **历史跨度**：2004 年至今（20年，跨越 2008 金融危机、2020 疫情、2022 加息等多个宏观周期）
- **策略逻辑**：基于价格行为的横盘收敛 + 箱体突破趋势跟踪
  - Edge 来源：横盘收敛 + 突破
  - 风险核心：小止损 + 右尾收益
  - 组合原则：风险等权 + heat 控制
  - 避免过拟合：不使用主观图形、不使用复杂事件逻辑
- **交付成果**：经稳健性验证的趋势跟踪策略的参数组合、全周期净值曲线、风险评估报告，以及可直接用于实盘的策略逻辑

### 1.2 趋势跟踪策略 1.0 - Baseline策略

该策略核心假设：资产价格经历充分横盘收敛并成功突破长期阻力位后，具有极强的趋势延续潜力。1.0 版本专注于**可系统化实现、可量化验证**的核心条件，复杂的判断逻辑延至 2.0 迭代（见第9节）。

#### A. 开仓条件

- **开仓条件**：若 close_price[t] > max(close_price[t-N+1 : t-1])，则在 t+1 日开盘价买入。参数：N = 100（baseline），需进行敏感性测试：80 / 120 / 252
- **流动性约束**：使用 rolling window 计算历史流动性：ADV_60 = 过去 60 个交易日的平均日成交额（Average Daily Dollar Volume），仅纳入满足以下条件的标的：ADV_60 > $20M。所有流动性指标仅使用当时在t日可获得的历史数据计算，严禁使用全历史均值或未来数据，避免 look-ahead bias

#### B. 开仓执行

- **开仓价：** entry_price = open_price[t+1] × (1 + slippage)。跳空过滤（防滑点）：若 abs(open_price[t+1] / close_price[t] - 1) > 2.5%，则放弃该信号（阈值后面再调）。
- **开仓仓位：** raw_position = (总资产 × 1%) ÷ (entry_price − stop_loss)。极端情况保护：  
若 (entry_price − stop_loss) / entry_price < 0.5%，则跳过该交易。
- **单标的金额上限：** capped_position = 总资产 × 5% ÷ entry_price，final_position = min(raw_position, capped_position)
- **重复开仓限制**：同一标的在持仓期间不得重复开仓
- **组合层风险控制**：position_risk = final_position × (entry_price − stop_loss)；total_heat = sum(position_risk) / NAV；if total_heat > 10% of asset，then：不开新仓（不是减仓）
- **简单相关性削减** 
   - 计算时点：在 t 日开盘前执行，所有计算仅使用 t-1 及之前数据
   - 收益率定义：使用对数收益率：r_t = ln(close_t / close_{t-1})
   - 计算窗口：使用过去 60 个交易日：t-60 ~ t-1
   - 数据对齐与有效性：所有收益率序列按时间索引对齐（t-60 ~ t-1）逐日计算，若存在缺失值，使用重叠区间（inner join），若有效数据点少于 40，则不计算相关性
   - 相关性计算：对齐时间序列后，使用 Pearson correlation：corr = cov(r_x, r_y) / (std(r_x) × std(r_y))，若 std = 0，则不计算相关性（或设为 0）
   - 计算逻辑：对于每一个新开仓标的 new_asset，计算其与当前所有持仓标的两两相关性，定义：max_corr = max(corr(new_asset, asset_i) for asset_i in existing_positions) 
   - 仓位调整规则：若：max_corr > 0.7，则：final_position *= 0.5
   - 特殊情况处理：若当前无持仓，则跳过相关性削减，若数据不足（<40天），则跳过该相关性计算（或统一跳过交易）
- **手续费和滑点** 手续费：3 bps，滑点：10 bps

#### C. 止损

- ATR(20) is recomputed daily using rolling window on close prices up to t-1
- **止损价格**：stop_loss = entry_price - 2 × ATR(20)[t-1]  其中 ATR 使用 t-1 日收盘数据计算，避免未来函数。参数 2 为基准值，需进行敏感性测试（1.5 / 2 / 2.5 / 3）。
- **止损信号生成**：在每日收盘后判断：if close_price[t] < stop_loss，则生成止损信号
- **止损执行**：在 t+1 日开盘价执行卖出

#### D. 止盈

每日收盘后更新：
- 更新最高价：highest_high[t] = max(highest_high[t-1], high_price[t])
- 更新止盈线：trail_stop[t] = highest_high[t] − 2 × ATR(20)[t-1]
在 t+1 日开盘前执行判断：if close_price[t] < trail_stop[t]：在 t+1 open 平仓
- 若同一交易日同时触发止损与止盈信号：优先执行止损（Stop Loss 优先级高于 Trailing Stop）
- 初始状态：开仓完成后，highest_high 初始化为 entry_price

#### E. 空仓资金

自动配置到短债 ETF（如SGOV），不参与择时。exit_stock → same day or next open → buy SGOV

#### 执行一致性原则

- 所有 price[t] 指代 t 日收盘后可获得信息
- 所有信号（开仓 / 止损 / 止盈）在 t 日收盘生成，并在 t+1 日开盘执行
- 本策略采用 end-of-day signal + next-open execution 模式，不模拟 intraday 价格路径

#### 信号执行顺序

- 每个交易日收盘后（t 日），基于当日收盘数据进行以下操作：
     - 更新持仓状态（如 highest_high、trail_stop 等）
     - 判断所有平仓信号（止损与止盈）
     - 生成新的开仓信号
- 所有交易指令在下一交易日开盘（t+1）统一执行：
     - 平仓指令优先于开仓指令执行
     - 同一标的在同一时点不会同时开仓与平仓
- 本策略采用 end-of-day signal + next-open execution 模式，避免使用任何未来数据

#### 执行价格模型

所有交易统一使用以下成交模型：

- 买入成交价：entry_price = open_price[t+1] × (1 + slippage)
- 卖出成交价：sell_price = open_price[t+1] × (1 - slippage)
- 手续费：3 bps（双边计入）
- 滑点：10 bps（单边计入）

---

## 2. 数据策略与质量保障

### 2.1 核心数据需求
- **标的数据**：美股所有市值 > 20 亿美元的个股 + 全量 ETF，历史日线 OHLCV 数据（**必须包含已退市股票**）
- **衍生数据**：动态市值（Point-in-time）、行业分类、财报发布日期

### 2.2 数据来源推荐

| 来源 | 质量 | 价格 | 推荐场景 |
|---|---|---|---|
| **Polygon.io** | 好，含退市股，API友好 | ~$29/月 | ✅ 研究阶段首选 |
| **Norgate Data** | 极好，专业级，含完整退市股 | ~$270/年 | ✅ 预算充足时优先 |
| **Yahoo Finance** | 差，有幸存者偏差，数据质量不稳定 | 免费 | ❌ 不可用于严肃回测 |
| **QuantConnect Data** | 好 | 按量计费 | 配合 LEAN 实盘使用 |
| **Sharadar (via Nasdaq Data Link)** | 极好，含基本面 | ~$100/月 | 市值/财报日期过滤 |

**推荐组合**：Polygon.io（价格数据）+ Sharadar（市值/财报日期数据）

### 2.3 幸存者偏差防御（关键）

**必须包含已退市股票。** 若回测仅基于当前仍交易的股票，会因忽略历史上大量破产/被收购的失败案例，导致绩效虚高 **20%~50%**。趋势跟踪策略受幸存者偏差影响尤为严重，因为"成功突破后大涨"的案例大多来自至今仍在市的股票。

### 2.4 数据完整性规范（Enhanced Data Integrity Spec）

| 规范项 | 要求 |
|---|---|
| **退市股处理** | 所有数据集必须包含退市股；退市原因不同，收益记录方式不同：破产清算 → 最终价格收益率（通常约 -100%）；并购/私有化 → 使用 point-in-time acquisition price 作为最终成交价；退市后无报价 → 不允许隐式延伸最后价格 |
| **ETF inception date 与关闭处理** | 每个回测时间点的 ETF 候选池只包含该日期之前已发行的 ETF（按 inception date 过滤）；ETF 清盘关闭后须在关闭日从 universe 移除，最终收益按最终 NAV 计算（等同于退市处理）；inception date / closure date 须来自可靠来源（如 Polygon.io ETF details 端点），不可用当前全量 ETF 列表倒推历史 |
| **公司行为映射** | 拆股、并购、更名等须维护幸存者一致的连续符号映射；所有拆股、分红、并购价格必须完整反映到价格序列（fully price-adjusted），避免公司行为产生虚假突破信号 |
| **数据缺失处理** | 缺失 bar 不允许隐式前向填充（forward-fill）；须明确标记为不可交易日（`is_tradable[t] = False`）；**信号生成与订单执行两个阶段均须强制跳过**：信号不计算、止损/止盈条件不判断、新订单不提交，直到下一个可交易 bar |
| **流动性时效约束** | ADTV 须用**截至 t-1 日（不含当日）**的滚动历史窗口计算：`ADTV[t] = mean(volume[t-30 : t-1])`；若使用 `volume[t-30 : t]`（含当日），则引入未来流动性信息，属于 look-ahead bias |
| **基本面数据时效** | 所有基本面数据（市值、财报等）须为 Point-in-time，并在官方发布日后延迟**至少1个交易日**才可使用 |
| **执行时效** | 所有交易均在信号触发后的**下一个可用开盘价**执行，严禁使用当日收盘价（日内 Look-ahead bias） |
| **数据清洗** | 清除缺失观测值、零成交量异常值、未复权导致的极端价格跳动 |
| **复权方式** | 所有价格使用**复权后收盘价**（分红、拆股调整）；市值筛选使用 Point-in-time 数据，不可用当前市值倒推历史 |

---

## 3. 策略稳健性与鲁棒性测试

> ⚠️ **重要：测试必须按以下顺序执行，不可颠倒。顺序错误会导致在噪声中优化，结论无效。**

### 3.0 推荐执行顺序

```
Step 1：跑 Baseline（N=100，ATR止损/止盈），获得初始净值曲线
Step 2：Ablation study（逐条件增量测试），与 Baseline 逐项对比
Step 3：Monte Carlo（return bootstrap + block bootstrap）
       验证基准参数下策略有 genuine 正期望，再进入参数搜索
       ⚠️ MC 必须在 heatmap 之前：否则等于在噪声结构上做参数优化
Step 4：参数热力图（N × ATR倍数 × 相关性阈值），确认高原区存在
Step 5：Execution 扰动（滑点 + 延迟），验证对执行摩擦成本的鲁棒性
Step 6：Walk-Forward（expanding window，OOS 不重叠），最终 OOS 验证
```

---

### 3.1 参数敏感性分析（参数热力图）

使用 vectorbt 进行网格搜索，生成双参数交叉热力图。1.0策略参数仅以下几个，参数少是防止过拟合的第一道防线：

| 参数 | 基准值 | 测试范围 | 目的 | 优先级 |
|---|---|---|---|---|
| **突破回望窗口 N** | 100日 | 80 / 100 / 120 / 252 | 信号频率 vs 趋势质量的权衡 | ⭐⭐⭐ 必测 |
| **ATR止损倍数** | 2.0 | 1.5 / 2.0 / 2.5 / 3.0 | 盈亏比 vs 止损过紧导致频繁出场 | ⭐⭐⭐ 必测 |
| **相关性阈值** | 0.7 | 0.5 / 0.6 / 0.7 / 0.8 / 0.9 | 组合分散度 vs 信号过滤强度 | ⭐⭐⭐ 必测 |
| ATR止损周期 | 20日 | 10 / 15 / 20 / 30 | 止损对波动响应速度的敏感度 | ⭐⭐ |
| ATR止盈（highest）周期 | 20日 | 10 / 15 / 20 / 30 / 50 | 趋势利润留存能力 | ⭐⭐ |
| ATR止盈倍数 | 2.0 | 1.5 / 2.0 / 2.5 / 3.0 | 截断速度 vs 趋势持续持有 | ⭐⭐ |
| 单标的金额上限 | 5% | 3% / 5% / 8% / 10% | 集中度 vs 分散化 | ⭐⭐ |
| 仓位风险比例（per trade risk） | 1% NAV | 0.5% / 1% / 1.5% / 2% | 每笔风险敞口大小 vs 组合波动率 | ⭐⭐ |
| 跳空过滤阈值（Gap filter） | 2.5% | 1% / 2.5% / 5% | 防滑点过滤强度 vs 信号损失 | ⭐ |
| 相关性惩罚强度 | ×0.5 | ×0.25 / ×0.5 / ×0.75 / ×1.0（无惩罚） | 相关标的的仓位压缩力度 | ⭐ |
| 相关性计算窗口 | 60日 | 20 / 40 / 60 / 90 / 120 | 相关性估计稳定性 vs 响应速度 | ⭐ |
| 滑点（slippage） | 10 bps | 5 / 10 / 20 / 30 bps | 验证策略对执行成本的鲁棒性 | ⭐ |
| 手续费（commission） | 3 bps | 0 / 1 / 3 / 5 bps | 手续费敏感度（主要影响高频信号） | ⭐ |

> **时间有限时只测优先级 ⭐⭐⭐ 的三个参数**：N × ATR止损倍数 × 相关性阈值，先确认高原区后再扩展至其他参数。

**稳定性量化指标（合格标准）：**

| 指标 | 计算方式 | 合格标准 |
|---|---|---|
| **Flatness Index** | mean(top-20% 参数组性能) ÷ max(所有参数组性能) | **> 0.85**；越接近 1 表示高原越宽；低于此值则为孤峰 |
| **Parameter Stability Score** | std(top-20% 参数组性能) ÷ mean(top-20% 参数组性能) | **< 0.15**；衡量高原区内部离散度 |
| **参数邻域衰减率** | (最优值 − 邻近参数均值) ÷ 最优值 | **< 20%** 可接受；**> 40%** 为高风险过拟合 |

---

### 3.2 噪声注入测试（Price Perturbation）

对历史价格序列加入微小随机扰动，重新跑完整回测：

```
price' = price × (1 + ε)
ε ~ N(0, σ)，σ 分别取 0.1%、0.2%、0.3%
```

**判定标准：**
- CAGR 下降幅度应 **< 20~30%**
- 若 0.1% 的轻微噪声就导致策略崩溃 → **高度怀疑过拟合**，需重新审视信号逻辑

---

### 3.3 蒙特卡洛模拟

两种方式结合使用：
- **Return bootstrap**（推荐）：对每日收益率序列有放回地重采样，模拟 1000 条等长收益路径；保留单日收益的统计分布特征，不破坏整体时序结构。**注：不应使用 trade-level shuffle（打乱交易顺序）——该方法会破坏时序依赖，在金融序列中属于错误做法。**
- **Block bootstrap**：按月/季度分块随机重排，保留块内短期自相关结构，比 return bootstrap 更接近真实市场自相关特性；两者互为补充

**判定指标：**
- 最大回撤的 95th percentile（极端情景下的风险暴露）
- CAGR 分布的 10th percentile（最差情景下仍能接受）
- 策略容量上限评估（在目标资金规模下是否仍有正期望）

---

### 3.4 Walk-Forward 验证（最终验证）

采用 **Expanding Window**（非 rolling）设计：

| 要素 | 规格 |
|---|---|
| **Window 类型** | Expanding：IS 窗口从固定起点（2004）延伸至当前切割点，每折 IS 数据量递增 |
| **折数** | 约 8–10 折（取决于 OOS 段长度设定） |
| **IS / OOS 比例** | 约 60% IS / 40% OOS（每折独立计算） |
| **参数重拟合** | 每折 IS 窗口独立重新优化参数，不复用前一折结果 |
| **OOS 段重叠** | 相邻折 OOS 段不重叠（disjoint），避免同一时段被重复计入 OOS 验证 |

- **合格标准**：各折 OOS 平均绩效 ≥ 对应 IS 绩效的 **60%**
- 若 OOS 持续远低于 IS → 参数过多或模型过拟合，需简化策略结构

---

## 4. 机构级策略验证协议（Institutional Validation Protocol v2.0）

> 本节为 Phase 4 稳健性审计通过后的**进阶验证层**，专为 $1000万+ 量级实盘部署设计。所有指标须在第3节测试全部通过后方可执行。

---

### 4.1 防过拟合统计检验

#### 4.1.1 Deflated Sharpe Ratio（DSR）

用于修正参数搜索带来的"虚假 Sharpe"与多次试验后的 selection bias：

```
DSR = PSR(SR_observed | SR_expected_max, T, skew, kurtosis)
```

其中 `SR_expected_max` 基于参数搜索总次数 K 估算（搜索越多，期望最大 Sharpe 越高）。

| DSR | 解释 |
|---|---|
| > 1.5 | 强策略，修正后仍高度显著 |
| 1.0–1.5 | 可接受 |
| < 1.0 | 很可能是过拟合，不建议实盘 |

#### 4.1.2 Probabilistic Sharpe Ratio（PSR）

```
PSR = P(SR_true > SR_benchmark)
```

| PSR | 解释 |
|---|---|
| > 0.95 | 强统计显著 |
| 0.90–0.95 | 可接受 |
| < 0.90 | Sharpe 估计不可信 |

#### 4.1.3 多重检验修正（Multiple Testing Correction）

本策略做了 N × ATR × 相关性阈值三维参数扫描，等价于多次假设检验，须进行显著性修正：

- **Bonferroni 修正**（保守）：`adjusted p = raw_p × K`，K 为参数搜索总次数
- **Holm 修正**（推荐）：对 K 个 p-value 从小到大排序，逐步修正
- **记录要求**：必须记录参数搜索总次数 K，作为 DSR 的计算输入

---

### 4.2 市场环境分层验证（Regime Validation）

全样本回测掩盖了策略在不同环境下的实际表现，必须分 4 段独立评估：

#### 4.2.1 四大市场环境

| Regime | 时间段 | 特征 |
|---|---|---|
| **金融危机** | 2008–2009 / 2020 Q1 | 极端跌幅 + 流动性危机 |
| **长牛趋势** | 2009–2019 | 持续上涨 + 低波动率 |
| **通胀冲击** | 2022 | 货币紧缩 + 成长股杀估值 |
| **低波动横盘** | 2013–2017 | 低波动 + 指数缓慢上涨 |

#### 4.2.2 每个 Regime 独立评估

每个环境下须单独计算：Sharpe、最大回撤、胜率、尾部收益分布

#### 4.2.3 合格标准

```
Sharpe_regime_min ≥ 0.6 × Sharpe_total
```

- **一票否决**：任一 regime 下 Sharpe < 0（持续亏损），整体验证不通过
- 说明：若不满足，策略为 regime-dependent alpha，在真实市场中不稳定

---

### 4.3 容量与市场冲击分析（Capacity Analysis）

策略在目标资金规模下须具备可行的执行能力：

#### 4.3.1 资金容量模型

```
capacity ≈ ADV × participation_rate × holding_period
```

#### 4.3.2 参与率假设

| 资产类型 | 最大参与率 |
|---|---|
| 大盘股 | 10%–15% |
| 中盘股 | 5%–10% |
| ETF | 15%–30% |

#### 4.3.3 检查点

| 检查项 | 要求 |
|---|---|
| **规模可扩展性** | 验证 $1000万 / $5000万 / $1亿 规模下策略是否可行 |
| **滑点非线性** | 资金规模增大时，滑点是否出现非线性爆炸 |
| **流动性限制** | 单笔下单量 ≤ 标的 ADTV 的 1%~2% |

---

### 4.4 收益来源分解（Return Decomposition）

验证策略 Alpha 来自可解释的结构性 edge，而非隐性因子暴露：

#### 4.4.1 Alpha 来源拆解

| 来源 | 说明 |
|---|---|
| **突破收益** | 开仓后前 N 日的 P&L 贡献 |
| **趋势延续** | N 日后持仓阶段的 P&L 贡献 |
| **波动率扩张** | 突破后波动率放大带来的收益放大效应 |

#### 4.4.2 因子暴露回归（Beta Exposure）

```python
returns ~ β_market   × SPY_returns
        + β_sector   × sector_factor
        + β_momentum × momentum_factor
```

必须计算并报告：市场 beta、动量 beta、波动率 beta

#### 4.4.3 合格标准

```
alpha / total_return > 0.6
```

- 若不满足：策略主要来自因子 timing，而非真实的策略 alpha

---

### 4.5 稳定性综合评分（Stability Index）

将多维稳健性测试压缩为单一可追踪评分：

#### 4.5.1 Stability Score（SS）

```
SS = 0.25 × Regime_Score
   + 0.25 × Parameter_Score
   + 0.25 × MC_Score
   + 0.25 × OOS_Score
```

| 子评分 | 计算方式 |
|---|---|
| **Regime_Score** | 4 个 regime 中 Sharpe ≥ 0.6×total 的比例（0 / 0.25 / 0.5 / 0.75 / 1.0） |
| **Parameter_Score** | Flatness Index > 0.85 且邻域衰减率 < 20% = 1.0（已在 3.1 节定义） |
| **MC_Score** | MC 模拟 10th percentile CAGR > 5% = 1.0 |
| **OOS_Score** | Walk-Forward 各折 OOS ≥ IS 60% 的比例（已在 3.4 节定义） |

| SS | 解释 |
|---|---|
| > 0.8 | 机构级，可实盘部署 |
| 0.6–0.8 | 可用但脆弱，需持续监控 |
| < 0.6 | 仅供研究，不可实盘 |

---

### 4.6 最终放行门控（Final Gate）

策略进入实盘部署前，须**同时满足**以下全部条件（任一不满足则不放行）：

| 指标 | 合格条件 |
|---|---|
| **DSR** | > 1.0 |
| **PSR** | > 0.95 |
| **Regime 最低 Sharpe** | ≥ 0.6 × 全样本 Sharpe |
| **MC 最差 Sharpe** | 10th percentile > 0 |
| **策略容量** | 目标 AUM 规模下可行执行 |
| **Stability Score** | SS > 0.75 |

> ⚠️ **Final Gate 是硬性一票否决制**：任何一项不满足，整体验证失败，需重新优化策略，不得绕过或降低标准。

---

## 5. 技术栈架构

**核心技术栈决策（研究阶段）：**

| 工具 | 角色 | 理由 |
|---|---|---|
| **Backtrader** | 主回测引擎 | 逐K线执行，支持复杂状态机逻辑（多次触顶、动态止盈等），纯Python，易于调试 |
| **QuantStats** | 性能分析与报告 | 专业基金级报告，净值图/回撤图/Sharpe等一键生成 |
| **vectorbt** | 参数扫描（辅助） | 向量化加速，用于子参数的网格搜索（止损幅度、横盘窗口等） |
| **LEAN / lean-cli** | 生产实盘（未来） | 策略成熟后迁移，对接券商API，机构级执行。研究阶段不引入，避免额外复杂度 |

### 5.1 三层架构设计

```
┌──────────────────────────────────────────────────────────────┐
│  Layer 3：报告层                                              │
│  QuantStats — 净值图、回撤图、Sharpe、Calmar、月度热力图等   │
├──────────────────────────────────────────────────────────────┤
│  Layer 2：回测执行层（核心）                                  │
│  Backtrader — 逐K线执行，复杂止损/止盈状态机                 │
├──────────────────────────────────────────────────────────────┤
│  Layer 1：信号生成 + 参数扫描层                               │
│  自定义Python (pandas/numpy) — 图形识别、横盘检测、信号生成  │
│  vectorbt（辅助）— 子参数快速网格搜索                        │
└──────────────────────────────────────────────────────────────┘
```

### 5.2 各工具职责说明

**Backtrader（主回测引擎）**
- 逐K线模拟执行，真实反映进出场时序
- 实现多次触顶检测、横盘状态追踪等复杂状态机逻辑
- 内置手续费、滑点模型
- 使用Python multiprocessing实现并行化，补偿单线程速度短板
- **适合本策略的核心原因**：策略逻辑是状态机驱动而非纯向量化，`if/else` 实现方式远比强行向量化更清晰可维护

**QuantStats（分析报告层）**
- 接收任何来源的收益率序列，生成专业HTML/PDF报告
- 净值曲线（vs SPY基准）、水下回撤图、月度收益热力图
- 夏普率、索提诺率、卡玛比率、最大回撤、平均回撤时长等

**vectorbt（参数扫描辅助）**
- **不用于实现完整策略逻辑**（状态机逻辑不适合向量化）
- 用于子参数的快速网格搜索：止损幅度（0.3%~1%）、横盘振幅阈值、移动止盈窗口等
- 可并行测试数千种参数组合，生成参数热力图，识别稳定"高原区"

**LEAN / lean-cli（生产阶段，暂不引入）**
- 策略在Backtrader验证成熟后，考虑迁移至LEAN
- 对接Interactive Brokers等券商API，实现实盘执行
- 提供机构级滑点建模和风险管理

---

## 6. 逐步实施路线图

1.0阶段专注于构建**极简、可验证的纯价格动量基准**：52周新高突破 + ATR动态止损/止盈。所有图形识别、财报过滤、窄止损等复杂条件均延至2.0（见第9节），确保先建立干净基准，再逐条叠加测试增量效果。

### Phase 1：数据基础与基准建立（第1-2周）

**任务：**
- 搭建本地数据库（PostgreSQL 或 DuckDB）
- 对接Polygon.io API，下载2004年至今全量数据（含退市股，严格按2.4规范处理）
- 实现动态市值过滤（Point-in-time，> $20亿）
- 搭建Backtrader基础框架，实现完整Baseline策略：
  - 开仓：`if close[t-1] > max(close[t-N : t-1]): buy at open[t]`，N = 100
  - 止损：`stop_loss = entry_price - 2 × ATR(20)[t-1]`
  - 动态止盈：`trail_stop = highest(close, 10) - 2 × ATR(20)`，`close < trail_stop` 时次日开盘出场
  - 仓位：每笔初始风险 = 总资产 × 1%，单标的上限 = 总资产 × 5%

**产出：** Baseline净值曲线（vs SPY），作为后续所有优化的对比基准

---

### Phase 2：突破前形态过滤（第3周）

**任务：**
- 实现突破前高位横盘/回调形态过滤（至少2个bar高位横盘，或回调最低价 > 横盘中位价）

**产出：** 加入突破前形态过滤后的回测结果，与Baseline对比增量效果；作为1.0完整策略基准，供2.0各条件逐项叠加对比

---

### Phase 3：滑点建模（第4周）

**任务：**
- 添加滑点建模（按目标资金规模估算市场冲击成本）

**产出：** 完整1.0策略基准，含真实成本建模，供2.0逐条件叠加对比

---

### Phase 4：稳健性审计（第5-7周）

严格按**第3节规定的执行顺序**完成以下测试（顺序不可颠倒）：

1. 参数热力图 → 对 N、ATR周期、ATR倍数做网格扫描，确认存在"高原区"及邻域衰减率达标
2. 噪声注入测试 → 验证价格微扰下的稳定性
3. 蒙特卡洛模拟（交易级 + Block级）
4. 执行扰动测试 → 验证对滑点和延迟的鲁棒性
5. Walk-Forward 验证 → 最终跨期稳健性验证
6. 生成 Edge Stability Map（见8.4节）

**产出：** 完整1.0策略可行性证明文档，准备进入2.0迭代

---

## 7. 环境部署与开发流程

### 7.1 安装依赖

```bash
pip install backtrader
pip install quantstats
pip install vectorbt
pip install pandas numpy matplotlib
pip install requests  # Polygon.io API
```

### 7.2 数据管道

```bash
# 1. 初始化本地数据库
python data/init_db.py

# 2. 下载历史数据（含退市股，2004至今）
python data/download_polygon.py --start 2004-01-01 --universe us_large_cap

# 3. 每日增量更新
python data/update_daily.py
```

### 7.3 回测执行

```bash
# 运行单次回测
python backtest/run.py --strategy phase1_baseline --start 2004-01-01 --end 2024-12-31

# 生成QuantStats报告
python reports/generate.py --results backtest/results/phase1_baseline.pkl

# 参数网格搜索（vectorbt）
python optimize/grid_search.py --param lookback --range 40,60,80,100
```

### 7.4 标准工作流

1. 编写/修改策略逻辑（`strategies/` 目录）
2. 运行 Backtrader 回测，输出逐笔交易日志
3. 用 QuantStats 生成性能报告，与Baseline对比
4. 用 vectorbt 执行网格搜索，生成参数热力图，确认高原区及邻域衰减率
5. 按顺序执行：噪声注入 → 执行扰动 → 蒙特卡洛 → Walk-Forward

---

## 8. 回测输出内容规范

每个Phase完成后，自动生成完整审计数据包：

### 8.1 核心绩效指标

| 类别 | 指标 |
|---|---|
| 风险回报 | 夏普比率、索提诺比率、卡玛比率 |
| 收益 | 年化收益率（CAGR）、月度收益分布、vs SPY超额收益 |
| 风险 | 最大回撤、平均回撤、回撤持续时长、年化波动率 |
| 交易统计 | 胜率、盈亏比、数学期望、平均持仓天数、年均交易次数 |
| 容量评估 | 不同资金规模（$100万/$1000万/$5000万/$1亿）下的滑点冲击估算 |

### 8.2 可视化图表
- **净值曲线**：策略累计收益 vs SPY基准（对数坐标）
- **水下回撤图**（Underwater Plot）：历史回撤压力时序图
- **月度收益热力图**：12×N年度的绩效分布矩阵
- **参数敏感性热图**：双参数交叉的性能分布（高原/孤峰判断）
- **持仓特征分布**：行业分布、持仓时长分布、单笔盈亏分布（R倍数）

### 8.3 审计日志
- **完整交易明细**：每笔交易的标的、入场日期/价格、出场日期/价格/原因、实际盈亏（R倍数）
- **QuantStats HTML报告**：完整的专业基金审计格式报告

### 8.4 Edge Stability Map（CTA高阶分析）

在Phase 4稳健性审计完成后，额外生成以下四维分析，用于理解策略的真实盈利来源和脆弱点：

| 分析维度 | 内容 | 目的 |
|---|---|---|
| **① 年份收益分解（Regime Decomposition）** | 逐年CAGR贡献，区分牛市/熊市/震荡市年份 | 识别策略在哪种市场环境下赚钱，哪种环境下亏损 |
| **② 行业Alpha贡献** | 各GICS行业的交易笔数、胜率、平均R倍数 | 识别哪些行业是真正的Alpha来源，是否依赖少数行业 |
| **③ 趋势长度与收益集中度** | 持仓天数分层（<20日 / 20-60日 / 60日+）的P&L贡献 | 验证"80%收益来自哪些趋势长度"，指导止盈参数设计 |
| **④ 突破贡献 vs 持有贡献** | 将每笔交易P&L拆分为：突破阶段（入场后前N日）vs 趋势持有阶段 | 判断策略优势是"突破选股"还是"趋势持有" |

> **用途**：Edge Stability Map 揭示策略是否有真实的市场微观结构支撑，还是依赖特定历史时期的偶然因素。若行业或年份过度集中，需重新评估策略普适性。

---

## 9. 策略 2.0 待测清单

以下条件在1.0中被简化，待1.0基准确立后逐项加入并独立测试增量效果。对于一下每个条件，做开关对比回测，或者参数扫描。

### 9.1 开仓条件：
#### 大盘位置
- **大盘位置过滤：** 根据大盘位置（突破区/顶部/底部）调整开仓风险敞口
#### 上涨趋势强度
- **均线牛市排列：** 三均线（MA20/50/200）大致呈牛市排列（图形质量高时非完美排列可接受）
- **52周新高过滤：** 突破K线须为近一年内最高价
#### 横盘形态
- **横盘收敛（核心 edge）：** 过去80个交易日最高/最低价波幅收敛至20%以内，确保横盘充分，需要做参数扫描（窗口40/60/80/100日，幅度15%/20%/25%/30%）。参考实现逻辑：range = (max(high[t-80:t-1]) - min(low[t-80:t-1])) / min(low[t-80:t-1]) range < 0.25
- **触顶次数过滤假突破（核心 edge）：** 阻力位被触碰至少3次（触顶价 ≥ 阻力位90%，顶部连续横盘算1次），需要做参数扫描（2次 vs 3次） 。参考实现逻辑：touch_count ≥ 3（允许±2%误差）
- **横盘长宽比下限：** 排除过短横盘的低质量突破（具体最小bar数，需要做参数扫描） 
- **突破前形态：** 突破前至少2个bar高位横盘，或回调最低价 > 横盘中位价
#### 突破强度
- **突破强度（动能确认）：** 大阳柱突破（涨幅 > 1.5%）或跳空高开突破（突破阳线一半须在阻力线以上），需要做参数扫描（幅度0.5%/1%/1.5%/2%/3%） 
- **禁止开仓：开盘价偏低：** 开仓日开盘价 < 突破K线开盘价 或 < 阻力位价格，则不开仓，加入后对比净效果
- **禁止开仓：跳空过大：** 跳空高开幅度超过阈值则不追（阈值待测试），需要做参数扫描。参考程序实现：if open[t] / close[t-1] - 1 > 2.5%: skip
#### 财报处理
- 财报前：通过移动止盈自然触发或主动减仓离场
- 财报后：若财报前1天卖出且财报日涨跌幅不大，可考虑重新买入（需全量数据系统测试，验证是否有显著正期望）
- 财报安全窗口：开仓日距下次财报日 ≥ 20个交易日；距财报 < 10日时 position size × 0.5 | 参数扫描（窗口15/20/30日）
#### 其他
- **严重插针过滤：** 历史上有严重插针的标的，止损可靠性低

### 9.2 止损
- 触发条件 A：止损放置于突破前小幅横盘/回调最低价下方 0.5%
- 触发条件 B：止损放置于突破前最后跳空上涨阶段的跳空K线低点下方 0.5%
- **设计原则**：尽量设小止损——虽然胜率会降低，但盈亏比会显著提高

### 9.3 平价保护
- 触发条件 A：开仓后第二次高点高出现后，将止损上移至开仓价
- 触发条件 B：浮盈超过 1倍R（初始风险），立即设置平价保护
- 触发条件 C：提前触发（财报前），临近财报日且走势未明朗时，提前设置平价保护（需系统测试验证效果）

### 9.4 多阶段移动止盈
- 首次上调：出现高点高后，移动止盈放置于近期最低点或开仓K线低点下方0.5%
- 谨慎模式触发（满足任一）：已上调2次止盈后 / 出现一波暴涨后
- 跳空锁利：出现跳空高开或临近财报，将移动止盈放到跳空K线下方

### 9.5 动态仓位、相关性与风险管理
- **空仓期资金配置**：策略信号稀少时，冗余资金自动配置于短期美债ETF（**BIL / SGOV**），获取无风险收益并补偿资金成本；出现新信号时快速切换
- **风险等权重（Risk Parity）**：根据个股ATR动态调整仓位，平衡组合波动贡献
- **行业集中度控制（Total Heat）**：单行业所有持仓"止损金额之和 / 总资产" < 4%；全账户Total Heat上限12%
- **相关性控制**：新标的与现有持仓相关性 > 0.7 时自动减半仓位；相关性 > 0.8 时（即使跨行业）也减半
- **Beta中性监控**：组合Beta > 1.5时动态降低持仓热度

### 9.6 宏观择时与黑天鹅熔断
- **黑天鹅熔断**：SPY单日跌幅 < -3% 时立即减仓50%；< -5% 时全局清仓进入避险模式
- **大盘择时开关（Market Regime）**：基于SPY 200日均线动态调整总持仓上限
- **流动性限制**：单笔下单量 ≤ 标的日均成交量（ADTV）的1%~2%

### 9.7 交易执行与滑点防御
- **开盘滑点保护**：开盘价较前收盘价偏离 > ±2% 时暂缓执行
- **冷静期策略**：开盘后5~15分钟不成交，或改用VWAP算法成交
- **分段止盈**：显著浮盈后采用阶梯式止盈，平滑净值曲线

### 9.8 标的池门槛升级（$5000万+资金规模）
- 市值门槛：$20亿 → **$50亿**（专注大中盘绩优股）
- 价格门槛：股价 **> $10**（严防低价股虚假突破与归零风险）
- 流动性门槛：日均成交额（ADTV）**> $1000万**

### 9.9 标普500指数增强策略（S&P 500 Index Enhancement）

**核心结构：Core-Satellite**

在长期持有 SPY 核心仓位的基础上，引入趋势跟踪策略作为战术性 Alpha 卫星配置，提升组合收益率与风险调整后回报：

| 市场环境 | 策略行为 |
|---|---|
| **牛市 / 趋势行情** | 卫星仓位捕捉个股/ETF 趋势 Alpha，实现相对 SPY 的超额收益 |
| **震荡 / 熊市** | 卫星仓位触发止损退出，资金回流 SPY；通过快速止损降低回撤，同时维持市场基准暴露 |

**资金流转规则：**
- 趋势系统产生开仓信号时：从 SPY 中按风险预算释放资金（risk-budgeted reallocation），进入 Satellite positions（个股/ETF 趋势仓位）
- Satellite position 触发退出信号时：释放资金不做择时判断，统一回流至 SPY

---

## 10. 策略 2.0 优化步骤

> **核心原则：每次只加一个条件，先找到真正有 edge 的 entry，再优化止损结构，最后才做组合与执行层优化。**

### 10.1 策略 2.0 draft版本（等待测试）

#### Entry（4个条件，仅此而已）

**条件1：基础突破（Baseline）**
```
close[t-1] > max(close[t-N : t-1])
N = 100
```

**条件2：横盘收敛（核心 edge）**
```
range = (max(high[t-80:t-1]) - min(low[t-80:t-1])) / min(low[t-80:t-1])
range < 0.25
```

**条件3：触顶次数（过滤假突破）- 暂时去掉touch_count，先验证是否真有增量**
```
touch_count ≥ 2（允许 ±2% 误差）
```

**条件4：突破强度（动能确认）**
```
(close[t-1] / open[t-1] - 1) > 1%
```

> 没有 MA、没有财报、没有复杂过滤。

#### Entry Execution（极简但关键）

```
开仓：在 t 日开盘价买入

# 跳空保护（唯一执行过滤）
gap = open[t] / close[t-1] - 1

# 默认
skip_trade = False
risk_multiplier = 1.0

if gap > 0.05:
    skip_trade = True
elif gap > 0.025:
    risk_multiplier = 0.5

# 可选：弱 breakout 处理（建议后面测试）
elif gap < -0.02:
    skip_trade = True
```

#### Stop Loss（核心风险控制）

使用**结构止损**（不是 ATR）：

```
initial_stop   = entry - 2 × ATR(20)
structure_stop = lowest(low, 20) * 0.995

stop = max(initial_stop, structure_stop)
```

> 这是整个系统最关键的 alpha 放大器之一。

#### Position Sizing（风险等权）

```
risk_per_trade = 1% of equity
position_size  = risk_per_trade / (entry - stop)
max_position   = 5% of equity          # 单标的上限
```

#### Portfolio Risk Control（组合稳定核心）

```
# Total Heat（必须有）
sum(all position risk) ≤ 10% of equity

# 超过则：不开新仓（不是减仓）
```

- 需要增加行业 / 因子 exposure 控制（Sector / Factor Overlay）

#### Exit（只保留一个机制）

Trailing Stop（唯一止盈）：
```
trail = highest(high, 20) - 2 × ATR(20)
if close[t] < trail:
    exit at next open
```

#### 现金管理

空仓资金：自动配置到短债 ETF（BIL / SGOV），不参与择时

#### 参数敏感性分析范围
| 模块 | 参数 | 测试值 |
|---|---|---|
| **Entry** | 突破回望窗口 N | 80 / 100 / 120 |
| **Entry** | 横盘收敛窗口 | 60 / 80 / 100 日 |
| **Entry** | 横盘收敛阈值 | 20% / 25% / 30% |
| **Exit** | Trailing window | 20 / 30 / 50 日 |
| **Exit** | ATR 倍数 | 2 / 3 |

---
### 10.2 策略 3.0 和之后的进一步改进步骤

#### Step 1：找到最优 Entry（最关键）

在 1.0 Baseline 上，依次叠加并独立验证以下开仓条件：

| 条件 | 对应 9.1 条目 |
|---|---|
| 横盘收敛（形态过滤） | 形态过滤（横盘收敛） |
| 触顶次数 | 阻力触碰次数 |
| 突破强度 | 突破强度过滤 |
| 52周新高 | 52周新高过滤 |

**目标**：找到让 entry 真正有 edge 的条件组合（Sharpe 提升 + 回撤降低 + 全子周期有效）

---

#### Step 2：优化止损与止盈结构

在 Step 1 最优版本基础上，测试：

| 条件 | 说明 |
|---|---|
| 窄止损（structure stop） | 突破前低点下方 0.5%，替代 ATR 止损 |
| 多阶段 trailing stop | 高点高后上移止盈，谨慎模式触发逻辑 |

**目标**：找到收益分布最优解（盈亏比 × 胜率 × 持仓时长分布）

---

#### Step 3：组合稳定性优化

在 Step 2 最优版本基础上，加入：

| 条件 | 说明 |
|---|---|
| Risk Parity | 按个股 ATR 动态调整仓位，平衡波动贡献 |
| Total Heat 上限 | 全账户止损敞口 ≤ 总资产 12% |
| 相关性削减 | 新仓与现有持仓相关性 > 0.7 时减半仓位 |

**目标**：组合层面的回撤控制与稳定性

---

#### Step 4：市场环境与执行层优化（最后）

| 条件 | 说明 |
|---|---|
| Market Regime 过滤 | SPY 200日均线位置调整总持仓上限 |
| 黑天鹅熔断 | SPY 单日跌幅触发减仓/清仓 |
| 执行优化 | 跳空保护、冷静期、VWAP 成交 |

---

#### 筛选标准（每加一个条件必须通过以下三问）

每叠加一个新条件，须同时满足：

| 问题 | 合格标准 |
|---|---|
| 是否提升 Sharpe？ | 夏普比率提升（不只看收益绝对值） |
| 是否降低最大回撤？ | 最大回撤持平或改善 |
| 是否在所有子周期都有效？ | 2004–2010、2010–2020、2020–今 各段均有正贡献 |

**一票否决规则**：出现以下任意一条，直接放弃该条件，不进入下一步：

- 只在特定历史区间有效（如仅 2010–2020）
- 参数极其敏感（邻域衰减率 > 40%）
- 提升收益但同时扩大最大回撤

---

## 11. 关键风险提示

1. **幸存者偏差**：使用不含退市股数据的回测结果不可信，严禁用Yahoo Finance作为主数据源
2. **过度拟合**：参数超过5个时，必须做Walk-Forward验证；邻域衰减率 > 40% 是明确的过拟合警告信号
3. **测试顺序错误**：若在确认参数高原之前就做Walk-Forward，等于在噪声中优化，结论无效
4. **滑点低估**：大额资金下，小市值股的市场冲击成本可能使回测结果虚高20%~30%，必须在回测中真实建模
5. **Look-ahead bias**：市值筛选、财报日期等信息必须使用Point-in-time数据；所有交易必须使用信号后次日开盘价执行
6. **研究阶段 ≠ 实盘**：Backtrader回测结果满意后，实盘前仍需在LEAN框架中做独立的out-of-sample验证
