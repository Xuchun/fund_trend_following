# 趋势跟踪策略 - 研发与回测设计方案

## 1. 趋势跟踪策略1.0

### 1.0 背景
本方案旨在构建一个**趋势跟踪策略研发系统**，完成策略的设计、回测、调优与稳健性分析，最终服务于 $1000万美元以上量级的主动管理基金。

- **标的范围**：全量 ETF + 美股个股（市值 > 20 亿美元，日均成交额 > $20M，标的价格 > $10），必须使用**adjusted price（复权价格）** 来做历史回测
    - **个股**：必须包含**已退市股票** ，防止幸存者偏差。必须考虑公司行为，比如拆股/分红/合并
    - **ETF**：每个回测时间点只纳入该时间点之前已发行的ETF（按inception date过滤）
- **历史跨度**：2004 年至今（22年，跨越 2008 金融危机、2020 疫情、2022 加息等多个周期）
- **策略核心假设**
      资产价格成功突破前前期高点后，具有较强的趋势延续潜力。
- **策略逻辑**：
    - Edge 来源：突破历史前高 + 趋势延续
    - 风险核心：小止损 + 右尾收益
    - 组合原则：风险等权 + heat 控制
    - 避免过拟合：不使用主观图形、不使用复杂事件逻辑
    - 不使用资金杠杆
- **交付成果**：
      可用于实盘交易的趋势跟踪策略 1.0，以及策略的参数组合、全周期净值曲线、稳健性验证、风险评估。策略 1.0 专注于可系统化实现、可量化验证的baseline策略，复杂的进一步策略优化延至未来版本的迭代。

### 1.1 趋势跟踪策略 1.0 - Baseline策略

#### 基本原则

  - price[t] 指代 t 日收盘后可获得信息
  - 下面的所有计算，在 t 日收盘后进行，仅使用 t 日及之前的数据
  - 所有信号（开仓 / 止损 / 止盈）在 t 日收盘生成，并在 t+1 日开盘执行
  - 价格约定：除 min_price 过滤器使用原始收盘价外，本节所有 close_price、high_price、low_price、open_price 均指复权价格（adjusted price）

#### A. 开仓条件

- **流动性约束：仅纳入满足 `ADV_60[t] > $20M` 的标的**

  ADV_**60**[t] = average(dollar_volume[t-59 : t])

  表示过去60个交易日的平均日成交金额（Average Daily Dollar Volume）。ADV_60[t]使用截至 t 日收盘的历史成交金额（包含 t 日）

- **开仓条件：当日收盘价突破过去 N 日（不含当日）的最高最高价（high price）**

  rolling_high_N[t] = rolling_max(daily_high_price, window=N, shift=1)
  breakout_signal[t] = close_price[t] > rolling_high_N[t]

  若：
      breakout_signal[t]
      and close_price[t] > 10 USD
      and market_cap[t] > 2B USD
      and ADV_60[t] > 20M USD
      and volume[t] ≥ 1.5 × mean(volume[t-60:t-1])

  则在 t+1 日开仓。

  参数缺省值：
    - Breakout window N = 200
    - Price threshold = 10 USD
    - Market cap filter = 2B USD
    - ADV filter = 20M USD
    - Mean volume (shares) in X days  = 60

#### B. 开仓执行

- **市场环境过滤器**

  当 SPY 收盘价 > 其 200 日简单移动平均线 时， 策略处于「牛市模式」，允许开仓。否则进入「熊市模式」，停止新建仓位。

    - 牛市模式（Bull）：SPY close_price[t] > SMA(200)[t]  → 正常扫描入场信号
    - 熊市模式（Bear）：SPY close_price[t] ≤ SMA(200)[t]  → pending_entries = []

- **跳空高开过滤**

  upper_band = close_price[t] × (1 + **2.5%**)
  lower_band = close_price[t] × (1 - **2.5%**)

  若 t+1 开盘价超出该区间，则：不执行该交易信号

  参数缺省值：Gap filter = 2.5%

- **止损价**

  stop_loss = entry_price - **2** × ATR(**20**)[t]

  其中，True Range (TR)[i] 定义为：
  TR[i] = max(
      high[i] - low[i],
      abs(high[i] - close[i-1]),
      abs(low[i] - close[i-1])
  )

  ATR(20) 使用 Wilder's smoothing method 计算：ATR[i] = (ATR[i-1] × 19 + TR[i]) / 20，ATR 初始值为前20个 TR 的简单平均

  ATR(20)[t] 表示：使用截至 t 日的全部历史数据递推计算得到的 ATR 值，在 t 日收盘时已完全可知

  参数缺省值：Stop loss multiplier = 2.0，ATR length = 20

- **止损距离太近**

  若 (entry_price − stop_loss) / entry_price < **0.5%** ，则放弃该开仓信号

  参数缺省值：Min stop distance = 0.5% 

- **原始开仓仓位**

  raw_position = (总资产 × **1%**) ÷ (entry_price − stop_loss)

  参数缺省值：Risk per trade = 1% NAV

- **单标的仓位上限 & 初步开仓仓位**

  capped_position      = 总资产 × **5%** ÷ entry_price
  preliminary_position = min(raw_position, capped_position)

  参数缺省值：Position cap = 5% NAV 

- **相关性过滤**
  - 收益率：使用对数收益率 r[t] = ln(close_price[t] / close_price[t-1])
  - 数据对齐与样本选择：对于每一对资产（new_asset, asset_i），new_asset 是新开仓标的，asset_i 是当前持仓标的 
    - a) 获取两者的对数收益率序列 r_new 和 r_i
    - b) 按时间索引进行对齐（inner join）：
        - 仅保留两个资产在同一交易日均存在收益率数据的日期
        - 得到对齐后的收益率序列：{(t_k, r_new[t_k], r_i[t_k])}, k = 1 ... K
        - 时间索引按升序排列（t_1 < t_2 < ... < t_K）
    - c) 样本截断（取最近窗口）：
        - 从对齐后的序列中，取"时间上最新的最多**60**个观测值"
        - 即使用子序列：{(t_{K-m+1}, ..., t_K)}，其中 m = min(K, 60)
    - d) 有效性检查：若 m < **40**，则认为样本不足，不计算该资产对的相关性
    - e) 标准差检查：若任一资产在该样本窗口内收益率标准差为 0，则不计算该资产对的相关性
    - f) 在该截断后的样本上计算 Pearson correlation
  - 相关性计算：采用 Pearson correlation of log returns，corr(x, y) = cov(r_x, r_y) / (std(r_x) × std(r_y))
  - 组合相关性规则：对于每一个新开仓标的 new_asset，计算其与当前所有持仓标的 asset_i 的两两相关性，定义：max_corr = max(corr(new_asset, asset_i) for asset_i in current_positions)（仅考虑正相关；corr 为负时自然低于阈值，视为不相关，不触发减仓——负相关在多头组合中具有对冲效果）
  - 仓位调整规则：final_position = preliminary_position 是默认值。若：max_corr > **0.7**，则：final_position = preliminary_position × **0.5**
  - 特殊情况处理：
    - 若 current_positions 为空，则final_position = preliminary_position
    - 若无任何有效相关性计算结果 → 视为 max_corr = 0，则 final_position = preliminary_position

  参数缺省值：Correlation window = 60d，Correlation threshold = 0.7  

- **组合层风险控制**

  new_position_risk = final_position × (entry_price − stop_loss)
  total_heat        = (existing_risk + new_position_risk) / NAV
  existing_risk 为当前持仓（t 时点）所有仓位的风险金额之和
  若 total_heat > **10%** ，则：放弃该开仓信号（不是减仓）

  参数缺省值：Heat limit = 10%  

- **多信号处理**
  - 多个不同标的的开仓信号按顺序逐个处理，按 breakout_strength 降序排序，breakout_strength = close_price[t] / max(high_price[t-N:t-1])
  - 每执行一笔后立即更新 current_positions 和 existing_risk，后续开仓信号的相关性计算与 total_heat 判断，均基于更新后的投资组合状态

- **每成功开仓一笔交易后**
  - 更新 current_positions
  - 更新 existing_risk = existing_risk + new_position_risk
  - 对于这笔开仓，highest_high 初始化为 entry_price（不使用 t+1 日盘中最高价，以避免引入未来信息）

#### C. 止损

- **止损信号生成** ：在持仓阶段，每日收盘后判断：若当天的 low_price[t] < stop_loss，则生成止损信号

- **止损执行**：在 t+1 日开盘执行平仓

- **优先级规则（如果同时存在多个退出信号）**：
    Exit priority (highest priority first):
    1. stop_loss
    2. trailing_stop
    3. take_profit

#### D. 止盈

- trail_stop[t0] = entry_price − 3 × ATR[t0]

- **每日收盘后更新**

  highest_high[t] = max(highest_high[t-1], high_price[t])

  R = entry_price - stop_loss
  R_multiple[t] = (highest_high[t] - entry_price) / R

  if R_multiple[t] < 1:
    trail_stop[t] = max(trail_stop[t−1], highest_high[t] − **3** × ATR(**20**)[t])
  elif R_multiple[t] < 3:
    trail_stop[t] = max(trail_stop[t−1], highest_high[t] − **3** × ATR(**20**)[t])
  else:
    trail_stop[t] = max(trail_stop[t−1], highest_high[t] − **5** × ATR(**20**)[t])

- **在 t 日收盘后执行判断**

  if close_price[t] < trail_stop[t]：在 t+1 日开盘执行平仓

参数缺省值：Trailing stop multiplier = 3.0 / 3.0 / 5.0，ATR length = 20

开仓初期，移动止盈初始值为 entry_price − 3×ATR，低于止损 entry_price − 2×ATR，故止损先提供保护。当 highest_high > entry_price + 1×ATR 时，trail_stop = highest_high − 3×ATR > entry_price − 2×ATR，移动止盈自动超过止损，成为约束性条件。此接管时机由数学自然保证，代码无需显式实现。

#### E. 空仓资金

- 空仓资金在 t+1 日开盘，采用下面的"执行价格模型"买入 SHY，且 SHY 不参与策略信号计算与相关性计算

- SHY returns are included in NAV calculation and compounded daily

#### F. 信号执行顺序

- 每个交易日收盘后（t 日），基于当日收盘数据进行以下操作：
    - 更新持仓状态（如 highest_high、trail_stop 等）
    - 判断所有平仓信号（止损与止盈）
    - 生成新的开仓信号
- 所有交易指令在下一交易日开盘（t+1）统一执行：
    - 平仓指令优先于开仓指令执行
    - 同一标的在同一时点不会同时开仓与平仓

#### G. 执行价格模型

所有交易统一使用以下模型：

- 执行价格模型
    entry_price = open_price[t_entry+1] × (1 + slippage_bps / 10000)（滑点已含入成交价）
    exit_price  = open_price[t_exit+1] × (1 − slippage_bps / 10000)
    commission  = notional × commission_bps / 10000（单独扣除）

- 交易成本模型
    position = 成交后实际交易数量（filled shares）

    notional_in  = entry_price × position
    notional_out = exit_price × position

    buy_cost  = notional_in  × commission_bps / 10000   （3 bps，滑点已含入 entry_price）
    sell_cost = notional_out × commission_bps / 10000   （3 bps，滑点已含入 exit_price）

    transaction_cost = buy_cost + sell_cost

- PnL计算
    PnL = position × (exit_price - entry_price) - transaction_cost

参数缺省值：Commission = 3 bps，Slippage = 10 bps

---

### 1.2 策略 1.0 历史回测

⚠️ **重要：测试必须按以下顺序执行，不可颠倒。顺序错误会导致在噪声中优化，结论无效。**

#### 1.2.1 Baseline run

Baseline 不是“最优参数”，而是“代表性参数 + 稳健参数区间”。也就是说：
  - Baseline anchor（锚点参数）：用来做所有实验基准
  - 稳健参数区间：用来验证策略是否是靠着参数的优化而展现出来好的performance，检验策略是否对参数敏感

**1.2.1.1 Baseline anchor（锚点参数）**

用下面的Baseline anchor（锚点参数），跑历史回测，得到策略的基准 performance。

| 参数 | 建议值 | 原因 |
|------|--------|------|
| Breakout window N | 200 | 中期动量标准窗口 |
| Price threshold | $10 | 避免低价噪声股 |
| Market cap | $2B | 大盘趋势稳定性 |
| ADV filter | $20M | 保证可交易性 |
| volume_filter_multiplier | 1.5 | 保证突破是有成交量支持的 |
| ATR length | 20 | volatility standard (1 month) |
| Stop loss multiplier | 2 × ATR | CTA经典风险单位 |
| Trailing stop multiplier | 3/3/5 × ATR | 保持 symmetric risk envelope |
| Min stop distance | 0.5% | 避免 micro-noise stop |
| Risk per trade | 1% NAV | CTA标准单位 |
| Position cap | 5% NAV | 控制单票风险 |
| Heat limit | 10% | CTA safe drawdown envelope |
| Correlation threshold | 0.7 | 中等相关性过滤 |
| Correlation window | 60d | balance stability & responsiveness |
| Slippage                 | 10 bps   | 实盘摩擦敏感度       |
| Commission               | 3 bps    | 高频敏感性         |
| Gap filter               | 2.5%     | 跳空风险控制        |

**1.2.1.2 Deliverables**

    1️⃣ 核心绩效指标
      - CAGR
      - Sharpe
      - Max DD
      - Win rate
      - Avg win / Avg loss
      - Turnover（换手率）：组合一年大概“换手”多少次
      - Exposure（市场暴露）：资金有多少时间真的在市场里？

    2️⃣ Equity curve
      - 全周期净值曲线 + rolling Sharpe + rolling drawdown
      - 用来直观看：
        - 是否“靠一两段赚钱”
        - 是否长期停滞

    3️⃣ Trade-level stats
      - # trades
      - avg holding period
      - PnL distribution（偏态/肥尾）
      - largest win / largest loss
    
    4️⃣ Risk decomposition
      - by asset class（ETF vs stock）
      - by volatility regime

Baseline 的 deliverable 不是结论，而是：后面所有测试的“对照组”。

---
#### 1.2.2 Perturbation tests（local sensitivity）

**1.2.2.1 Perturbation tests 本质不是找最优参数**

**下面的 grid 不是用来找最优参数的，而是逐参数扰动测试 one-at-a-time perturbation**。例如：参数 N 的值变动，其它参数固定；或者参数 ATR length 的值变动，其它参数固定。然后看回测的表现：是否 flat region，是否 single peak。

| 参数                       | baseline | 测试范围（small perturbation grid） | 说明            |
| ------------------------ | -------- | ----------------------------- | ------------- |
| Breakout window N        | 200      | **[150, 170, 200, 230, 250, 270, 300]**   | 检测趋势周期敏感度 ✅ 已完成 |
| volume_filter_multiplier | 1.5×     | **[1.0, 1.2, 1.5, 2.0]**          | 突破日成交量须超过60日均量的倍数，过滤假突破 vs 入场频率 |
| Price threshold          | $10      | **[8, 10, 12, 15]**           | 低价噪声过滤强度      |
| Market cap               | $2B     | **[1B, 2B, 3B]**           | 大盘 vs 中盘稳定性   |
| ADV filter               | $20M     | **[10M, 20M, 30M, 50M]**      | 流动性约束         |
| ATR length               | 20       | **[10, 15, 20, 30]**          | 波动率估计窗口       |
| Stop loss multiplier     | 2 × ATR     | **[1.5, 2.0, 2.5, 3.0]**      | 风险紧/松平衡       |
| trail_multiplier_r1      | 3×ATR    | **[2.0, 2.5, 3.0, 3.5, 4.0]**     | 早期（<1R）移动止盈松紧，影响换手率与趋势发展空间 ✅ 已完成 |
| Min stop distance        | 0.5%     | **[0.3%, 0.5%, 1.0%]**        | 防止 noise stop |
| Risk per trade           | 1% NAV   | **[0.5%, 1%, 1.5%, 2%]**      | 单笔风险控制        |
| Position cap             | 5% NAV   | **[3%, 5%, 7%, 10%]**         | 集中度 vs 分散     |
| Heat limit               | 10%      | **[8%, 10%, 12%]**            | 系统级风险上限       |
| Correlation threshold    | 0.7      | **[0.5, 0.6, 0.7, 0.8, 0.9]** | 分散 vs 集中      |
| Correlation window       | 60d      | **[20d, 40d, 60d, 90d]**      | 稳定 vs 响应速度    |
| Slippage                 | 10 bps   | **[5, 10, 20, 30 bps]**       | 实盘摩擦敏感度       |
| Commission               | 3 bps    | **[0, 1, 3, 5 bps]**          | 高频敏感性         |
| Gap filter               | 2.5%     | **[1%, 2.5%, 5%]**            | 跳空风险控制        |

**1.2.2.2 Deliverables**

    1️⃣ 参数 <-> 绩效 曲线/表格

        每个参数一张表，会得到这种输出：参数 N 敏感性 （对于策略的历史回测的表现）

        | N   | CAGR  | Sharpe | Max DD |
        | --- | ----- | ------ | ------ |
        | 80  | 10.2% | 0.95   | -18%   |
        | 90  | 11.0% | 1.02   | -17%   |
        | 100 | 11.5% | 1.05   | -16%   |
        | 110 | 11.3% | 1.03   | -17%   |
        | 120 | 10.8% | 0.98   | -18%   |

    2️⃣ Sensitivity（敏感度）：看的是参数轻微变化 → 性能变化幅度

        ✔ good：变化 < ±10%
        ❌ bad：变化 > ±30–50%

        Sensitivity = std(performance) / mean(performance)

    3️⃣ Stability Region（稳定区间）：找的不是点，而是连续“可赚钱区间”

        ✔ good：N ∈ [90, 120] 都 ok
        ❌ bad：only N=100 works

  **1.2.2.3 结构性结论（必须写一段总结），例如：**

        - Breakout window exhibits wide plateau → robust
        - ATR multiplier shows moderate sensitivity → avoid extremes
        - Correlation threshold relatively stable in [0.6–0.8]

      Flatness（平坦性）：参数变化一点，策略的回测表现会不会崩？

        ✔ 好策略长这样：plateau（高原）
        N = 80–120 → performance almost same

        ❌ 坏策略长这样：尖峰
        N=100 → Sharpe 1.5
        N=90 → Sharpe 0.8
        N=110 → Sharpe 0.7

      常见错误：
        ❌ 用 perturbation 找最优参数
        ❌ 根据结果改策略（这会污染后面）

---
#### 1.2.3 Trade diagnostics

**gap_adjusted_loss_multiple 分析**

对于每一笔交易：
    - gap_adjusted_loss_multiple = (realized_exit_price - entry_price) / (entry_price - stop_loss)
    - realized_exit_price = actual fill price (including gap + slippage)

注意：realized_exit_price 用的是真实成交价（open[t+1] 或 worse fill），如果发现：
    - 平均 = 1.1R（正常）
    - 95% = 2.5R（危险）
    - max = 10R（灾难，说明：策略尾部风险严重 underestimated）
#### 1.2.4 蒙特卡洛模拟

**1.2.4.1 蒙特卡洛模拟（使用 Baseline anchor 锚点参数）**

两种方式结合使用：
- **Return bootstrap**：对每日收益率序列有放回地重采样，模拟 1000 条等长收益路径；保留单日收益的统计分布特征，不破坏整体时序结构。**注：不应使用 trade-level shuffle（打乱交易顺序）——该方法会破坏时序依赖，在金融序列中属于错误做法。**
- **Block bootstrap**：按月/季度分块随机重排，保留块内短期自相关结构，比 return bootstrap 更接近真实市场自相关特性；两者互为补充

还需要计算 **drawdown path dependency check（连续亏损长度分布）**。这很关键，因为 CTA 最怕：长时间不赚钱，导致投资人会赎回。

**判定指标：**
- 最大回撤的 95th percentile（极端情景下的风险暴露）
- CAGR 分布的 10th percentile（最差情景下仍能接受）
- Sharpe稳定性分布：Sharpe mean，Sharpe std，Sharpe skew
- 策略容量上限评估（在目标资金规模下是否仍有正期望）


**1.2.4.2 Deliverables**

1️⃣ **策略净值曲线** 

    输出：

        1000 条 simulated 策略净值曲线
        median 策略净值曲线
        5% / 95% band

    目的是看：

        是否存在大量“长期横盘”
        是否存在“资金死亡区”
        是否 recovery 极慢

2️⃣ **Drawdown 分布**

    输出：

        Median Max DD	
        95th percentile Max DD	
        Worst Max DD	

    重点：不是只看 drawdown 深度，还要看 drawdown duration（回撤持续时间）。因为很多 CTA 不是死于亏损，而是死于：“三年不创新高”。

3️⃣ **Drawdown path dependency check**

    输出：

        连续亏损长度	概率
        > 3 months	
        > 6 months	
        > 12 months	
        > 24 months	

        Avg underwater duration	
        95th percentile underwater duration	
        Longest underwater period	

    非常关键。很多策略 CAGR 很高，Sharpe 很好。但连续18个月不赚钱，实际募资会崩。

4️⃣ **Monte Carlo conclusion**

    结构性结论。例如：

        Strategy remains profitable under 90% of simulated paths
        Drawdown tail acceptable for CTA deployment
        Recovery duration may challenge investor retention
        Tail risk increases materially under block bootstrap

---
#### 1.2.5 Parameter heatmap（strict in-sample only）

⚠️ **重要：所有 heatmap 必须严格限制在 In the Sample (IS) 区间内，不得使用任何 Out of Sample (OOS) 数据。OOS 数据仅保留给 “2.6 Walk-Forward OOS 验证” 使用。Heatmap 不是用来选最优参数，而是用于识别 Parameter Stability Region（稳定区间）。**

| 参数                       | Baseline | Parameter Values            | 作用                  |
| ------------------------ | -------- | --------------------------- | ------------------- |
| Breakout window N        | 100      | 80 / 100 / 120 / 150        | 趋势周期稳定性             |
| Price threshold          | $10      | $8 / $10 / $12 / $15        | microcap污染敏感度       |
| Market cap filter        | 2B      | 1B / 2B / 3B             | universe稳定性         |
| ADV filter               | $20M     | $10M / $20M / $30M / $50M   | 流动性鲁棒性              |
| ATR length               | 20       | 10 / 15 / 20 / 30           | 波动率估计稳定性            |
| Stop loss multiplier     | 2.0      | 1.5 / 2.0 / 2.5 / 3.0       | 风险控制鲁棒性             |
| Trailing stop multiplier | 3.0      | 2.0 / 3.0 / 4.0 / 5.0       | 盈利退出稳定性             |
| Min stop distance        | 0.5%     | 0.3% / 0.5% / 1.0%          | 噪声敏感性               |
| Risk per trade           | 1% NAV   | 0.5% / 1% / 1.5% / 2%       | 波动 vs 收益放大          |
| Position cap             | 5% NAV   | 3% / 5% / 7% / 10%          | 集中度风险               |
| Heat limit               | 10%      | 8% / 10% / 12% / 15%        | 系统风险承载能力            |
| Correlation threshold    | 0.7      | 0.5 / 0.6 / 0.7 / 0.8 / 0.9 | 组合结构稳定性             |
| Correlation window       | 60d      | 20 / 40 / 60 / 90           | 相关性估计稳定性            |
| Slippage                 | 10 bps   | 5 / 10 / 20 / 30 bps        | execution fragility |
| Commission               | 3 bps    | 0 / 1 / 3 / 5 bps           | 成本敏感性               |
| Gap filter               | 2.5%     | 1% / 2.5% / 5%              | 开盘风险                |

Heatmap 设计原则：

- 仅使用 1D 和 2D heatmap，不进行 3D 及以上维度扫描（避免维度灾难与过拟合）
- 多参数分析采用：
    - Slice（固定部分参数）
    - Aggregation（跨参数平均）
    - Worst-case（极端情景）

**1D Heatmap（5个以内）**
- Breakout window N
- ATR length
- ATR stop loss multiplier 
- Trailing stop multiplier
- Min stop distance（可选）

**2D Heatmap（最多2个）**
- N × ATR stop loss multiplier（必须）
- N × Trailing stop multiplier（可选）

**Slice Tests（替代高维）**
固定参数 N × ATR（使用 baseline anchor：N = 100，ATR = 20）分别跑：
  - Correlation threshold：0.5 / 0.6 / 0.7 / 0.8 / 0.9
  - Risk per trade：0.5% / 1% / 1.5% / 2%
  - Market cap filter：1B / 2B / 3B
  - ADV filter：10M / 20M / 30M

**🧩 Parameter Stability Region 定义**

对于每个参数，必须满足：
  - Flatness（无尖峰）
  - Low Sensitivity（measured by CV = std(performance) / mean(performance)）
  - 连续区间（非孤立点）

**🎯 Final Parameter Selection Rule for OOS（唯一允许的方法）**

    Step 1：筛选稳定区间交集

      - Perturbation 稳定
      - Heatmap 无尖峰
      - Monte Carlo 分布可接受（CAGR 分布，Sharpe 分布，Max Drawdown 分布，Monte Carlo 10th percentile 情景下仍保持正收益）

      例如：
        - N ∈ [90, 120]
        - ATR ∈ [1.8, 2.5]
        - correlation ∈ [0.6, 0.8]

    Step 2：选取稳健内部点（robust interior point），参数方向必须偏保守：

      - 位于 Parameter Stability Region 中部区域
      - 不在局部 peak（avoid sharp maxima）

      例如：

      | 参数  | 区间    | 实盘选值   | 选择原则                              |
      | ---- | ------- | -------- | -----------------------------------  |
      | N    | 90–120  | **110**  | 稳定区间中间偏右（略慢周期），降低交易频率  |
      | ATR  | 1.8–2.5 | **2.2**  | 偏大一点（减少过早止损），降低噪声止损     |
      | corr | 0.6–0.8 | **0.65** | 偏低一点（更分散），增强 diversification |

    ❌ 明确禁止，不允许：
      - 从 Parameter Stability Region 中随机采样（no random sampling）
      - 多参数组合试验后再挑选（no post-selection）
      - 根据 OOS 表现回调参数（no feedback loop）

---
#### 1.2.6 Execution stress test（slippage, delay）

Execution Stress Test 是整套系统里非常关键的一步，因为它回答的不是“策略有没有alpha”，而是 alpha 在真实交易摩擦下还能不能活下来。很多CTA策略死在这里，不是因为逻辑错，而是因为回测过于理想化执行。Execution Stress Test 在不同滑点、延迟、流动性约束下，对策略进行执行层扰动建模，并评估其收益衰减曲线与结构性脆弱点，而不是单点回测结果变化。

至少 3 类：
  - slippage model stress（非线性滑点）
  - latency simulation（1-day / 2-day delay）
  - partial fill / liquidity constraint

**Deliverables**

1️⃣ Execution Sensitivity Curve（核心）

    | Stress Level | CAGR | Sharpe | Max DD | Turnover impact |
    | ------------ | ---- | ------ | ------ | --------------- |
    | Base         | 11%  | 1.05   | -16%   | 1.0x            |
    | Slippage     | 9.8% | 0.95   | -18%   | 1.0x            |
    | + Delay      | 8.7% | 0.88   | -20%   | 0.9x            |
    | Full stress  | 7.2% | 0.75   | -24%   | 0.7x            |

    👉 结论不是数字，而是：degradation curve（衰减曲线是否平滑）

2️⃣ Execution Fragility Map（执行脆弱性图）

    要回答：哪些参数最怕 execution stress？

    输出：
        N sensitivity under slippage
        ATR sensitivity under delay
        risk per trade sensitivity under fill

    本质是：second-order sensitivity analysis

3️⃣ Execution Robustness Verdict（最重要）

    最后必须给一句结构性结论，例如：

    Strategy remains robust under moderate slippage (≤15bps) but exhibits nonlinear degradation under delay >1 day, indicating sensitivity to timing rather than price impact.

    或者：

    Strategy is execution-fragile in low-liquidity regimes, suggesting capacity ceiling around mid-cap equity universe.

---
#### 1.2.7 Walk-Forward OOS 验证

这个策略是一个非平稳的自适应框架，其参数估计基于不断扩展的样本内（IS）数据进行更新。参数稳定性区域由IS数据确定，其定义方法（perturbation、heatmap及相关分析流程）保持一致，不引入OOS信息。每一次样本外（OOS）测试用于评估在该时刻重新估计得到的模型，并且整个过程不包含任何前瞻性信息。该策略的结构（突破逻辑、止损规则以及风险模型）保持不变；仅在参数稳定性区域内更新模型参数，用于后续 OOS 评估。

    参数训练区 IS 和 OOS 测试区间
        
        | IS        | OOS  | 
        | --------- | ---- |
        | 2004–2021 | 2022 |
        | 2004–2022 | 2023 | 
        | 2004–2023 | 2024 | 
        | 2004–2024 | 2025 |

    在每一个参数训练区 IS，做：
        - perturbation
        - heatmap
        - Monte Carlo
        - execution stress
        - 得到：Final Frozen Parameters

    然后在对应的 OOS 区间，评估：
        - CAGR retention
        - Sharpe retention
        - DD stability
        - turnover drift
        - exposure drift
        - OOS stitched equity curve

    把 2022 → 2026，真实串起来。这是：真实未来投资体验。

  🔁 再次强调
    - Execution stress test / Walk-forward OOS 仅用于验证
    - 如果失败：必须回到重构策略

---
#### 1.2.8 Regime robustness（市场环境鲁棒性）

本节在 IS 数据上进行 regime decomposition analysis，用于结构诊断而非参数优化，以识别策略收益在不同市场环境下的来源与稳定性。将 2004–2025 切分为以下市场环境：

| Regime        | 时间        |
| ------------- | --------- |
| 金融危机          | 2008–2009 |
| 牛市            | 2010–2019 |
| COVID shock   | 2020      |
| 加息熊市          | 2022      |
| recent regime | 2023–now  |

对每个 regime 计算以下指标，用于评估策略是否存在显著的 regime dependency（即收益是否集中于特定市场环境）。
    - CAGR
    - Sharpe
    - Max Drawdown

判断标准：
    - 是否仅在单一或少数 regime 中表现显著
    - 是否在特定 market regime 下出现系统性失效

例如，若仅在 2010–2021 表现显著，则可视为存在明显 regime overfitting 风险。

---

## 2. 趋势跟踪策略2.0

### 2.0 背景
本方案旨在构建一个**趋势跟踪策略研发系统**，完成策略的设计、回测、调优与稳健性分析，最终服务于 $1000万美元以上量级的主动管理基金。

- **标的范围**：全量 ETF + 美股个股（市值 > 20 亿美元，日均成交额 > $20M，标的价格 > $10），必须使用**adjusted price（复权价格）** 来做历史回测
    - **个股**：必须包含**已退市股票** ，防止幸存者偏差。必须考虑公司行为，比如拆股/分红/合并
    - **ETF**：每个回测时间点只纳入该时间点之前已发行的ETF（按inception date过滤）
- **历史跨度**：2004 年至今（22年，跨越 2008 金融危机、2020 疫情、2022 加息等多个宏观周期）
- **策略逻辑**：基于价格行为的横盘收敛 + 箱体突破的趋势跟踪
    - Edge 来源：横盘收敛 + 突破
    - 风险核心：小止损 + 右尾收益
    - 组合原则：风险等权 + heat 控制
    - 避免过拟合：不使用主观图形、不使用复杂事件逻辑
    - 不使用资金杠杆
- **策略核心假设**
    资产价格经历充分横盘收敛并成功突破长期阻力位后，具有极强的趋势延续潜力。策略1.0 版本专注于可系统化实现、可量化验证的baseline策略，复杂的进一步策略优化延至未来版本的迭代。
- **交付成果**：可用于实盘交易的趋势跟踪策略2.0，以及策略的参数组合、全周期净值曲线、稳健性验证、风险评估

### 2.1 策略 2.0 draft版本

#### Entry（4个条件，仅此而已）

**条件1：基础突破（Baseline）**
```
close[t-1] > max(close[t-N : t-1])
N = 100
```

**条件2：横盘收敛（核心 edge）**
```
range = (max(high[t-80:t-1]) - min(low[t-80:t-1])) / min(low[t-80:t-1])
range < 0.25
```

**条件3：触顶次数（过滤假突破）- 暂时去掉touch_count，先验证是否真有增量**
```
touch_count ≥ 2（允许 ±2% 误差）
```

**条件4：突破强度（动能确认）**
```
(close[t-1] / open[t-1] - 1) > 1%
```

> 没有 MA、没有财报、没有复杂过滤。

#### Entry Execution（极简但关键）

```
开仓：在 t 日开盘价买入

# 跳空保护（唯一执行过滤）
gap = open[t] / close[t-1] - 1

# 默认
skip_trade = False
risk_multiplier = 1.0

if gap > 0.05:
    skip_trade = True
elif gap > 0.025:
    risk_multiplier = 0.5

# 可选：弱 breakout 处理（建议后面测试）
elif gap < -0.02:
    skip_trade = True
```

#### Stop Loss（核心风险控制）

使用**结构止损**（不是 ATR）：

```
initial_stop   = entry - 2 × ATR(20)
structure_stop = lowest(low, 20) * 0.995

stop = max(initial_stop, structure_stop)
```

> 这是整个系统最关键的 alpha 放大器之一。

#### Position Sizing（风险等权）

```
risk_per_trade = 1% of equity
position_size  = risk_per_trade / (entry - stop)
max_position   = 5% of equity          # 单标的上限
```

#### Portfolio Risk Control（组合稳定核心）

```
# Total Heat（必须有）
sum(all position risk) ≤ 10% of equity

# 超过则：不开新仓（不是减仓）
```

- 需要增加行业 / 因子 exposure 控制（Sector / Factor Overlay）

#### Exit（只保留一个机制）

Trailing Stop（唯一止盈）：
```
trail = highest(high, 20) - 2 × ATR(20)
if close[t] < trail:
    exit at next open
```

#### 现金管理

空仓资金：自动配置到短债 ETF（BIL / SGOV），不参与择时

#### 参数敏感性分析范围

| 模块 | 参数 | 测试值 |
|---|---|---|
| **Entry** | 突破回望窗口 N | 80 / 100 / 120 |
| **Entry** | 横盘收敛窗口 | 60 / 80 / 100 日 |
| **Entry** | 横盘收敛阈值 | 20% / 25% / 30% |
| **Exit** | Trailing window | 20 / 30 / 50 日 |
| **Exit** | ATR 倍数 | 2 / 3 |

### 2.2 之后的章节参考 1.2

---

## 3. 策略后续开发

以下条件在前面的策略1.0和2.0中被简化。对于以下每个条件，做开关对比回测，或者参数扫描。

### 3.0 策略 3.0 和之后的进一步改进步骤

#### Step 1：找到最优 Entry（最关键）

在策略 2.0 上，依次叠加并独立验证以下开仓条件：

| 条件 | 对应 9.1 条目 |
|---|---|
| 横盘收敛（形态过滤） | 形态过滤（横盘收敛） |
| 触顶次数 | 阻力触碰次数 |
| 突破强度 | 突破强度过滤 |
| 52周新高 | 52周新高过滤 |

**目标**：找到让 entry 真正有 edge 的条件组合（Sharpe 提升 + 回撤降低 + 全子周期有效）

---
#### Step 2：优化止损与止盈结构

在 Step 1 最优版本基础上，测试：

| 条件 | 说明 |
|---|---|
| 窄止损（structure stop） | 突破前低点下方 0.5%，替代 ATR 止损 |
| 多阶段 trailing stop | 高点高后上移止盈，谨慎模式触发逻辑 |

**目标**：找到收益分布最优解（盈亏比 × 胜率 × 持仓时长分布）

---
#### Step 3：组合稳定性优化

在 Step 2 最优版本基础上，加入：

| 条件 | 说明 |
|---|---|
| Risk Parity | 按个股 ATR 动态调整仓位，平衡波动贡献 |
| Total Heat 上限 | 全账户止损敞口 ≤ 总资产 12% |
| 相关性削减 | 新仓与现有持仓正相关性 > 0.7 时减半仓位（负相关不触发） |

**目标**：组合层面的回撤控制与稳定性

---
#### Step 4：市场环境与执行层优化（最后）

| 条件 | 说明 |
|---|---|
| Market Regime 过滤 | SPY 200日均线位置调整总持仓上限 |
| 黑天鹅熔断 | SPY 单日跌幅触发减仓/清仓 |
| 执行优化 | 跳空保护、冷静期、VWAP 成交 |

---
#### 筛选标准（每加一个条件必须通过以下三问）

每叠加一个新条件，须同时满足：

| 问题 | 合格标准 |
|---|---|
| 是否提升 Sharpe？ | 夏普比率提升（不只看收益绝对值） |
| 是否降低最大回撤？ | 最大回撤持平或改善 |
| 是否在所有子周期都有效？ | 2004–2010、2010–2020、2020–今 各段均有正贡献 |

**一票否决规则**：出现以下任意一条，直接放弃该条件，不进入下一步：

- 只在特定历史区间有效（如仅 2010–2020）
- 参数极其敏感（邻域衰减率 > 40%）
- 提升收益但同时扩大最大回撤

---

### 3.1 开仓条件：

#### 大盘位置

- **大盘位置过滤：** 根据大盘位置（突破区/顶部/底部）调整开仓风险敞口

#### 上涨趋势强度

- **均线牛市排列：** 三均线（MA20/50/200）大致呈牛市排列（图形质量高时非完美排列可接受）
- **52周新高过滤：** 突破K线须为近一年内最高价

#### 横盘形态

- **横盘收敛（核心 edge）：** 过去80个交易日最高/最低价波幅收敛至20%以内，确保横盘充分，需要做参数扫描（窗口40/60/80/100日，幅度15%/20%/25%/30%）。参考实现逻辑：range = (max(high[t-80:t-1]) - min(low[t-80:t-1])) / min(low[t-80:t-1]) range < 0.25
- **触顶次数过滤假突破（核心 edge）：** 阻力位被触碰至少3次（触顶价 ≥ 阻力位90%，顶部连续横盘算1次），需要做参数扫描（2次 vs 3次） 。参考实现逻辑：touch_count ≥ 3（允许±2%误差）
- **横盘长宽比下限：** 排除过短横盘的低质量突破（具体最小bar数，需要做参数扫描）
- **突破前形态：** 突破前至少2个bar高位横盘，或回调最低价 > 横盘中位价

#### 突破强度

- **突破强度（动能确认）：** 大阳柱突破（涨幅 > 1.5%）或跳空高开突破（突破阳线一半须在阻力线以上），需要做参数扫描（幅度0.5%/1%/1.5%/2%/3%）
- **禁止开仓：开盘价偏低：** 开仓日开盘价 < 突破K线开盘价 或 < 阻力位价格，则不开仓，加入后对比净效果
- **禁止开仓：跳空过大：** 跳空高开幅度超过阈值则不追（阈值待测试），需要做参数扫描。参考程序实现：if open[t] / close[t-1] - 1 > 2.5%: skip

#### 财报处理

- 财报前：通过移动止盈自然触发或主动减仓离场
- 财报后：若财报前1天卖出且财报日涨跌幅不大，可考虑重新买入（需全量数据系统测试，验证是否有显著正期望）
- 财报安全窗口：开仓日距下次财报日 ≥ 20个交易日；距财报 < 10日时 position size × 0.5 | 参数扫描（窗口15/20/30日）

#### 其他

- **严重插针过滤：** 历史上有严重插针的标的，止损可靠性低

### 3.2 止损

- 触发条件 A：止损放置于突破前小幅横盘/回调最低价下方 0.5%
- 触发条件 B：止损放置于突破前最后跳空上涨阶段的跳空K线低点下方 0.5%
- **设计原则**：尽量设小止损——虽然胜率会降低，但盈亏比会显著提高

### 3.3 平价保护

- 触发条件 A：开仓后第二次高点高出现后，将止损上移至开仓价
- 触发条件 B：浮盈超过 1倍R（初始风险），立即设置平价保护
- 触发条件 C：提前触发（财报前），临近财报日且走势未明朗时，提前设置平价保护（需系统测试验证效果）

### 3.4 多阶段移动止盈

- 首次上调：出现高点高后，移动止盈放置于近期最低点或开仓K线低点下方0.5%
- 谨慎模式触发（满足任一）：已上调2次止盈后 / 出现一波暴涨后
- 跳空锁利：出现跳空高开或临近财报，将移动止盈放到跳空K线下方

### 3.5 动态仓位、相关性与风险管理

- **空仓期资金配置**：策略信号稀少时，冗余资金自动配置于短期美债ETF（**BIL / SGOV**），获取无风险收益并补偿资金成本；出现新信号时快速切换
- **风险等权重（Risk Parity）**：根据个股ATR动态调整仓位，平衡组合波动贡献
- **行业集中度控制（Total Heat）**：单行业所有持仓"止损金额之和 / 总资产" < 4%；全账户Total Heat上限12%
- **相关性控制**：新标的与现有持仓正相关性 > 0.7 时自动减半仓位（负相关不触发，视为对冲）；正相关性 > 0.8 时（即使跨行业）也减半
- **Beta中性监控**：组合Beta > 1.5时动态降低持仓热度

### 3.6 宏观择时与黑天鹅熔断

- **黑天鹅熔断**：SPY单日跌幅 < -3% 时立即减仓50%；< -5% 时全局清仓进入避险模式
- **大盘择时开关（Market Regime）**：基于SPY 200日均线动态调整总持仓上限
- **流动性限制**：单笔下单量 ≤ 标的日均成交量（ADTV）的1%~2%

### 3.7 交易执行与滑点防御

- **开盘滑点保护**：开盘价较前收盘价偏离 > ±2% 时暂缓执行
- **冷静期策略**：开盘后5~15分钟不成交，或改用VWAP算法成交
- **分段止盈**：显著浮盈后采用阶梯式止盈，平滑净值曲线

### 3.8 标的池门槛升级（$5000万+资金规模）

- 市值门槛：$20亿 → **$50亿**（专注大中盘绩优股）
- 价格门槛：股价 **> $10**（严防低价股虚假突破与归零风险）
- 流动性门槛：日均成交额（ADTV）**> $1000万**

### 3.9 标普500指数增强策略（S&P 500 Index Enhancement）

**核心结构：Core-Satellite**

在长期持有 SPY 核心仓位的基础上，引入趋势跟踪策略作为战术性 Alpha 卫星配置，提升组合收益率与风险调整后回报：

| 市场环境 | 策略行为 |
|---|---|
| **牛市 / 趋势行情** | 卫星仓位捕捉个股/ETF 趋势 Alpha，实现相对 SPY 的超额收益 |
| **震荡 / 熊市** | 卫星仓位触发止损退出，资金回流 SPY；通过快速止损降低回撤，同时维持市场基准暴露 |

**资金流转规则：**
- 趋势系统产生开仓信号时：从 SPY 中按风险预算释放资金（risk-budgeted reallocation），进入 Satellite positions（个股/ETF 趋势仓位）
- Satellite position 触发退出信号时：释放资金不做择时判断，统一回流至 SPY

---

## 4. 数据

### 4.1 核心数据需求

- **标的数据**：美股所有市值 > 20 亿美元的个股 + 全量 ETF，历史日线 OHLCV 数据（**必须包含已退市股票**）
- **衍生数据**：动态市值（Point-in-time）、行业分类

### 4.2 数据来源推荐

| 数据源                        | 价格（月）                                           | 历史覆盖            | Survivorship Bias            | 复权/公司行为                | ETF inception control | 粒度                  | API易用性  | 回测适配度（你的策略）            |
| -------------------------- | ----------------------------------------------- | --------------- | ---------------------------- | ---------------------- | --------------------- | ------------------- | ------- | ---------------------- |
| Polygon.io                 | $29–$199（Stocks Developer $79 常用）([Polygon][1]) | 10–20+ 年        | ⚠️ 部分支持（需自己构建 universe）      | ✔️ 支持 splits/dividends | ✔️ 可做但需自己过滤           | tick / 1min / EOD   | ⭐⭐⭐⭐⭐   | **8.5/10（性价比最高API方案）** |
| Norgate Data               | ~$50–$200（个人许可约 $30–$150区间，依套餐）                 | 30+ 年（强项）       | ✔️ 强（point-in-time universe） | ✔️ 极强（专业级调整）           | ✔️ 原生支持               | EOD（为主）             | ⭐⭐（偏文件） | **9.5/10（回测黄金标准）**     |
| Yahoo Finance              | 免费                                              | ~20年            | ❌ 严重问题                       | ⚠️ 不稳定                 | ❌ 无                   | EOD                 | ⭐⭐⭐     | **4/10（只适合原型）**        |
| QuantConnect               | $60–$200（平台订阅）                                  | 2000–至今（依资产）    | ✔️（Lean engine）              | ✔️ 内建调整                | ✔️ 内建                 | tick / minute / EOD | ⭐⭐⭐⭐    | **9/10（强回测系统，不只是数据）**  |
| Sharadar（Nasdaq Data Link） | $29–$199（Quandl bundle）                         | 1990s–至今（EOD为主） | ✔️（core US equity bundle）    | ✔️ adjusted EOD        | ✔️ 支持                 | EOD                 | ⭐⭐⭐⭐    | **8/10（因子+EOD强）**      |
| Tiingo                     | $10–$40（个人约 $10–$25）                            | ~15–20年         | ⚠️ 中等（早期数据质量一般）              | ✔️ adjusted close      | ❌ ETF过滤需自己做           | EOD / intraday      | ⭐⭐⭐⭐    | **7/10（性价比很好）**        |

[1]: https://polygon.io/pricing?utm_source=chatgpt.com "Pricing | Polygon"



| 来源                                  | 质量                                                                                                                                                                          | 价格                                       | 推荐场景                                                                   |
| ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- | ---------------------------------------------------------------------- |
| **Polygon.io**                      | 好                                                                                                                                                                           |                                          | ✅ 研究阶段首选                                                               |
| **Norgate Data**                    | **极好，完全支持关键需求** ✔ Survivorship-bias free US equities（包含退市股） ✔ adjusted OHLCV（拆股/分红/公司行为） ✔ 精确 delisted history ✔ sector / index membership history ✔ 可构建 2004+ 全市场 universe | ~$50–$150/月，USD360/年 👉 在“量化级数据”里属于极高性价比 | ✅ 预算充足时优先                                                              |
| **Yahoo Finance**                   | 差，有幸存者偏差，数据质量不稳定                                                                                                                                                            | 免费                                       | ❌ 不可用于严肃回测                                                             |
| **QuantConnect Data**               | 好，✔ US equities + ETF ✔ survivorship bias free universe ✔ 2004+（甚至更早） ✔ corporate actions fully adjusted ✔ factor/sector universe selection ✔ 可以直接实现你整个 strategy pipeline   | Free for research，$8–$60/月（pro） 👉 极高性价比 | 最大优势 👉 你不用自己搭 infra： 回测 engine（LEAN） 数据（Equities + ETFs），配合 LEAN 实盘使用 |
| **Sharadar (via Nasdaq Data Link)** | 极好，含基本面                                                                                                                                                                     | ~$100/月                                  | 市值/财报日期过滤                                                              |

**推荐组合**：Polygon.io（价格数据）+ Sharadar（市值/财报日期数据）

### 4.3 幸存者偏差防御（关键）

**必须包含已退市股票。** 若回测仅基于当前仍交易的股票，会因忽略历史上大量破产/被收购的失败案例，导致绩效虚高 **20%~50%**。趋势跟踪策略受幸存者偏差影响尤为严重，因为"成功突破后大涨"的案例大多来自至今仍在市的股票。

### 4.4 数据完整性规范（Enhanced Data Integrity Spec）

| 规范项 | 要求 |
|---|---|
| **退市股处理** | 所有数据集必须包含退市股；退市原因不同，收益记录方式不同：破产清算 → 最终价格收益率（通常约 -100%）；并购/私有化 → 使用 point-in-time acquisition price 作为最终成交价；退市后无报价 → 不允许隐式延伸最后价格 |
| **ETF inception date 与关闭处理** | 每个回测时间点的 ETF 候选池只包含该日期之前已发行的 ETF（按 inception date 过滤）；ETF 清盘关闭后须在关闭日从 universe 移除，最终收益按最终 NAV 计算（等同于退市处理）；inception date / closure date 须来自可靠来源（如 Polygon.io ETF details 端点），不可用当前全量 ETF 列表倒推历史 |
| **公司行为映射** | 拆股、并购、更名等须维护幸存者一致的连续符号映射；所有拆股、分红、并购价格必须完整反映到价格序列（fully price-adjusted），避免公司行为产生虚假突破信号 |
| **数据缺失处理** | 缺失 bar 不允许隐式前向填充（forward-fill）；须明确标记为不可交易日（`is_tradable[t] = False`）；**信号生成与订单执行两个阶段均须强制跳过**：信号不计算、止损/止盈条件不判断、新订单不提交，直到下一个可交易 bar |
| **流动性时效约束** | ADTV 须用**截至 t-1 日（不含当日）**的滚动历史窗口计算：`ADTV[t] = mean(volume[t-30 : t-1])`；若使用 `volume[t-30 : t]`（含当日），则引入未来流动性信息，属于 look-ahead bias |
| **基本面数据时效** | 所有基本面数据（市值、财报等）须为 Point-in-time，并在官方发布日后延迟**至少1个交易日**才可使用 |
| **执行时效** | 所有交易均在信号触发后的**下一个可用开盘价**执行，严禁使用当日收盘价（日内 Look-ahead bias） |
| **数据清洗** | 清除缺失观测值、零成交量异常值、未复权导致的极端价格跳动 |
| **复权方式** | 所有价格使用**复权后收盘价**（分红、拆股调整）；市值筛选使用 Point-in-time 数据，不可用当前市值倒推历史 |

### 4.5 分两步用不同数据来做历史回测

**第一步：使用 Yahoo Finance 数据来开发代码**

    Yahoo Finance 数据

    优点：
        - 免费
        - 具有我的趋势跟踪策略所需的历史数据

    缺点：

        - 很多退市股：没有，或者不完整
        - 公司行为数据不可靠：split，merger
        - ETF inception date 不可靠
        - 开盘价 occasionally 有脏数据

    需要根据 Yahoo Finance 提供的 adj_close，来自己计算 adjusted OHL:

    用 adj_close 生成 adj_factor = adj_close / Close，然后 adj_open = Open × adj_factor，adj_high = High × adj_factor，adj_low = Low  × adj_factor

**第二步骤：用商业高质量数据，用已开发的代码做策略的历史回测**



---

## 5. 技术栈架构

**核心技术栈决策（研究阶段）：**

| 工具                  | 角色       | 理由                                       |
| ------------------- | -------- | ---------------------------------------- |
| **Backtrader**      | 主回测引擎    | 逐K线执行，支持复杂状态机逻辑（多次触顶、动态止盈等），纯Python，易于调试 |
| **QuantStats**      | 性能分析与报告  | 专业基金级报告，净值图/回撤图/Sharpe等一键生成              |
| **vectorbt**        | 参数扫描（辅助） | 向量化加速，用于子参数的网格搜索（止损幅度、横盘窗口等）             |
| **LEAN / lean-cli** | 生产实盘（未来） | 策略成熟后迁移，对接券商API，机构级执行。研究阶段不引入，避免额外复杂度    |

### 5.1 三层架构设计

```
┌──────────────────────────────────────────────────────────────┐
│  Layer 3：报告层                                              │
│  QuantStats — 净值图、回撤图、Sharpe、Calmar、月度热力图等   │
├──────────────────────────────────────────────────────────────┤
│  Layer 2：回测执行层（核心）                                  │
│  Backtrader — 逐K线执行，复杂止损/止盈状态机                 │
├──────────────────────────────────────────────────────────────┤
│  Layer 1：信号生成 + 参数扫描层                               │
│  自定义Python (pandas/numpy) — 图形识别、横盘检测、信号生成  │
│  vectorbt（辅助）— 子参数快速网格搜索                        │
└──────────────────────────────────────────────────────────────┘
```

### 5.2 各工具职责说明

**Backtrader（主回测引擎）**
- 逐K线模拟执行，真实反映进出场时序
- 实现多次触顶检测、横盘状态追踪等复杂状态机逻辑
- 内置手续费、滑点模型
- 使用Python multiprocessing实现并行化，补偿单线程速度短板
- **适合本策略的核心原因**：策略逻辑是状态机驱动而非纯向量化，`if/else` 实现方式远比强行向量化更清晰可维护

**QuantStats（分析报告层）**
- 接收任何来源的收益率序列，生成专业HTML/PDF报告
- 净值曲线（vs SPY基准）、水下回撤图、月度收益热力图
- 夏普率、索提诺率、卡玛比率、最大回撤、平均回撤时长等

**vectorbt（参数扫描辅助）**
- **不用于实现完整策略逻辑**（状态机逻辑不适合向量化）
- 用于子参数的快速网格搜索：止损幅度（0.3%~1%）、横盘振幅阈值、移动止盈窗口等
- 可并行测试数千种参数组合，生成参数热力图，识别稳定"高原区"

**LEAN / lean-cli（生产阶段，暂不引入）**
- 策略在Backtrader验证成熟后，考虑迁移至LEAN
- 对接Interactive Brokers等券商API，实现实盘执行
- 提供机构级滑点建模和风险管理

---

## 6. 逐步实施路线图

1.0阶段专注于构建**极简、可验证的纯价格动量基准**：52周新高突破 + ATR动态止损/止盈。所有图形识别、财报过滤、窄止损等复杂条件均延至2.0（见第9节），确保先建立干净基准，再逐条叠加测试增量效果。

### Phase 1：数据基础与基准建立（第1-2周）

**任务：**
- 搭建本地数据库（PostgreSQL 或 DuckDB）
- 对接Polygon.io API，下载2004年至今全量数据（含退市股，严格按4.4规范处理）
- 实现动态市值过滤（Point-in-time，> $20亿）
- 搭建Backtrader基础框架，实现完整Baseline策略：
  - 开仓：`if close[t-1] > max(close[t-N : t-1]): buy at open[t]`，N = 100
  - 止损：`stop_loss = entry_price - 2 × ATR(20)[t-1]`
  - 动态止盈：`trail_stop = highest(close, 10) - 2 × ATR(20)`，`close < trail_stop` 时次日开盘出场
  - 仓位：每笔初始风险 = 总资产 × 1%，单标的上限 = 总资产 × 5%

**产出：** Baseline净值曲线（vs SPY），作为后续所有优化的对比基准

---
### Phase 2：突破前形态过滤（第3周）

**任务：**
- 实现突破前高位横盘/回调形态过滤（至少2个bar高位横盘，或回调最低价 > 横盘中位价）

**产出：** 加入突破前形态过滤后的回测结果，与Baseline对比增量效果；作为1.0完整策略基准，供2.0各条件逐项叠加对比

---
### Phase 3：滑点建模（第4周）

**任务：**
- 添加滑点建模（按目标资金规模估算市场冲击成本）

**产出：** 完整1.0策略基准，含真实成本建模，供2.0逐条件叠加对比

---
### Phase 4：稳健性审计（第5-7周）

严格按**第2节规定的执行顺序**完成以下测试（顺序不可颠倒）：

1. 参数热力图 → 对 N、ATR周期、ATR倍数做网格扫描，确认存在"高原区"及邻域衰减率达标
2. 噪声注入测试 → 验证价格微扰下的稳定性
3. 蒙特卡洛模拟（交易级 + Block级）
4. 执行扰动测试 → 验证对滑点和延迟的鲁棒性
5. Walk-Forward 验证 → 最终跨期稳健性验证
6. 生成 Edge Stability Map（见8.4节）

**产出：** 完整1.0策略可行性证明文档，准备进入2.0迭代

---

## 7. 环境部署与开发流程

### 7.1 安装依赖

```bash
pip install backtrader
pip install quantstats
pip install vectorbt
pip install pandas numpy matplotlib
pip install requests  # Polygon.io API
```

### 7.2 数据管道

```bash
# 1. 初始化本地数据库
python data/init_db.py

# 2. 下载历史数据（含退市股，2004至今）
python data/download_polygon.py --start 2004-01-01 --universe us_large_cap

# 3. 每日增量更新
python data/update_daily.py
```

### 7.3 回测执行

```bash
# 运行单次回测
python backtest/run.py --strategy phase1_baseline --start 2004-01-01 --end 2024-12-31

# 生成QuantStats报告
python reports/generate.py --results backtest/results/phase1_baseline.pkl

# 参数网格搜索（vectorbt）
python optimize/grid_search.py --param lookback --range 40,60,80,100
```

### 7.4 标准工作流

1. 编写/修改策略逻辑（`strategies/` 目录）
2. 运行 Backtrader 回测，输出逐笔交易日志
3. 用 QuantStats 生成性能报告，与Baseline对比
4. 用 vectorbt 执行网格搜索，生成参数热力图，确认高原区及邻域衰减率
5. 按顺序执行：噪声注入 → 执行扰动 → 蒙特卡洛 → Walk-Forward

---

## 8. 回测输出内容规范

每个Phase完成后，自动生成完整审计数据包：

### 8.1 核心绩效指标

| 类别 | 指标 |
|---|---|
| 风险回报 | 夏普比率、索提诺比率、卡玛比率 |
| 收益 | 年化收益率（CAGR）、月度收益分布、vs SPY超额收益 |
| 风险 | 最大回撤、平均回撤、回撤持续时长、年化波动率 |
| 交易统计 | 胜率、盈亏比、数学期望、平均持仓天数、年均交易次数 |
| 容量评估 | 不同资金规模（$100万/$1000万/$5000万/$1亿）下的滑点冲击估算 |

### 8.2 可视化图表

- **净值曲线**：策略累计收益 vs SPY基准（对数坐标）
- **水下回撤图**（Underwater Plot）：历史回撤压力时序图
- **月度收益热力图**：12×N年度的绩效分布矩阵
- **参数敏感性热图**：双参数交叉的性能分布（高原/孤峰判断）
- **持仓特征分布**：行业分布、持仓时长分布、单笔盈亏分布（R倍数）

### 8.3 审计日志

- **完整交易明细**：每笔交易的标的、入场日期/价格、出场日期/价格/原因、实际盈亏（R倍数）
- **QuantStats HTML报告**：完整的专业基金审计格式报告

### 8.4 Edge Stability Map（CTA高阶分析）

在Phase 4稳健性审计完成后，额外生成以下四维分析，用于理解策略的真实盈利来源和脆弱点：

| 分析维度 | 内容 | 目的 |
|---|---|---|
| **① 年份收益分解（Regime Decomposition）** | 逐年CAGR贡献，区分牛市/熊市/震荡市年份 | 识别策略在哪种市场环境下赚钱，哪种环境下亏损 |
| **② 行业Alpha贡献** | 各GICS行业的交易笔数、胜率、平均R倍数 | 识别哪些行业是真正的Alpha来源，是否依赖少数行业 |
| **③ 趋势长度与收益集中度** | 持仓天数分层（<20日 / 20-60日 / 60日+）的P&L贡献 | 验证"80%收益来自哪些趋势长度"，指导止盈参数设计 |
| **④ 突破贡献 vs 持有贡献** | 将每笔交易P&L拆分为：突破阶段（入场后前N日）vs 趋势持有阶段 | 判断策略优势是"突破选股"还是"趋势持有" |

> **用途**：Edge Stability Map 揭示策略是否有真实的市场微观结构支撑，还是依赖特定历史时期的偶然因素。若行业或年份过度集中，需重新评估策略普适性。

---

## 11. 关键风险提示

1. **幸存者偏差**：使用不含退市股数据的回测结果不可信，严禁用Yahoo Finance作为主数据源
2. **过度拟合**：参数超过5个时，必须做Walk-Forward验证；邻域衰减率 > 40% 是明确的过拟合警告信号
3. **测试顺序错误**：若在确认参数高原之前就做Walk-Forward，等于在噪声中优化，结论无效
4. **滑点低估**：大额资金下，小市值股的市场冲击成本可能使回测结果虚高20%~30%，必须在回测中真实建模
5. **Look-ahead bias**：市值筛选、财报日期等信息必须使用Point-in-time数据；所有交易必须使用信号后次日开盘价执行
6. **研究阶段 ≠ 实盘**：Backtrader回测结果满意后，实盘前仍需在LEAN框架中做独立的out-of-sample验证
