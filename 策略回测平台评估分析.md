# 策略回测平台评估分析

## 0. 评估前提：本策略的技术需求画像

工具选择不是通用性问题，而是"这个具体策略需要什么"的问题。先明确需求，再评估工具。

### 0.1 决定工具选择的核心约束

| 约束 | 具体要求 | 对工具的影响 |
|---|---|---|
| **标的规模** | 全量美股+ETF，约 3000–5000 个标的，22年日线数据 | 单次回测数据量巨大，速度是关键 |
| **状态机逻辑** | 每个持仓需独立追踪 highest_high、trail_stop、R-multiple 分段（2x/3x/5x ATR） | 逻辑无法向量化，必须逐K线顺序执行 |
| **顺序信号处理** | 同日多信号按 breakout_strength 排序，每执行一笔立即更新组合状态，下一笔用新状态计算相关性和 heat | 严格顺序依赖，天然反向量化 |
| **实时相关性过滤** | 每个新信号需与当前所有持仓算 Pearson correlation，当前持仓是动态变化的 | 不能预先批量计算，必须在事件循环中计算 |
| **参数扫描规模** | Perturbation tests（15个参数逐一扰动）+ Heatmap（1D/2D）+ Monte Carlo（1000条路径）+ Walk-Forward（4折） | 需要运行数百次甚至数千次完整回测 |
| **验证精度要求** | 机构级策略，任何 look-ahead bias 都会导致结论无效 | 不允许"近似实现"，必须逐笔精确 |

### 0.2 需求分层

```
层级 1：主回测引擎（最关键）
  → 实现策略逻辑，跑出单次回测结果
  → 要求：精确、正确、速度可接受

层级 2：参数扫描引擎（次关键）
  → 跑出数百次回测，识别参数稳定区间
  → 要求：速度快，允许简化（牺牲部分精度换速度）

层级 3：统计分析（Monte Carlo, Walk-Forward）
  → 基于主引擎结果做再采样和路径模拟
  → 要求：统计正确，Python 原生实现即可

层级 4：报告输出
  → 净值曲线、回撤图、月度热力图
  → 要求：专业美观，与引擎解耦
```

---

## 1. Backtrader 评估

### 1.1 基本信息

| 项目 | 状态 |
|---|---|
| 最后实质更新 | 约 2019–2020 年（GitHub 几乎停止维护） |
| PyPI 最新版本 | 1.9.78.123（2023年仅做了 minor fix，核心不变） |
| 社区活跃度 | 低，大量 issue 无人响应 |
| 定位 | 事件驱动型回测引擎，适合中小规模策略 |

### 1.2 能力评估

**能实现的部分：**
- 逐K线事件驱动循环 → 可以精确实现 end-of-day signal + next-open execution
- 每个持仓的状态机（highest_high、trail_stop 分段切换）→ 可以，这是 Backtrader 擅长的
- 止损/止盈优先级顺序 → 可以，通过 `Order.cancel()` 控制
- 内置 commission + slippage 模型 → 可以，简单配置

**实现起来很麻烦的部分：**
- **多标的相关性实时过滤**：Backtrader 没有组合层感知，需要自己在 `next()` 里手动维护当前持仓列表和收益率矩阵，代码量大且容易出错
- **顺序信号处理**：同一天多个信号按 breakout_strength 排序后逐个处理，Backtrader 的多 datafeed 设计没有原生支持这种跨标的排序逻辑
- **SGOV 现金管理**：每日计算剩余现金并买入 SGOV，在 Backtrader 里需要一个额外的 datafeed 和复杂的现金计算

**根本性的问题：速度**

Backtrader 的设计是针对**单一或少数标的**的研究型策略，不是为大规模 universe 优化的。

粗略估算：
```
标的数量   ：3000–5000 只
每只 K 线数：5500 bars（22年）
每 bar 操作：ATR 计算、状态更新、信号判断

单次回测耗时估算：30–90 分钟（Python 单线程）
参数扫描 100 组：50–150 小时
```

参数扫描完全不可行。即便用 `multiprocessing` 并行跑，每台机器 8 核也需要 6–20 小时跑完一轮。

### 1.3 综合评分

| 维度 | 评分 | 说明 |
|---|---|---|
| 策略逻辑实现正确性 | ★★★★☆ | 能实现，但相关性过滤需要大量自定义代码 |
| 大规模 universe 速度 | ★☆☆☆☆ | 致命短板，3000 只股票不可接受 |
| 工具维护状态 | ★★☆☆☆ | 事实上已停止维护，有隐患 |
| 调试友好度 | ★★★★★ | 纯 Python 逐行可追踪，这是最大优点 |
| 参数扫描支持 | ★★☆☆☆ | 无原生支持，multiprocessing 笨重 |
| **综合** | **★★☆☆☆** | **不推荐作为主引擎** |

**结论：** Backtrader 是技术上可行但工程上不合理的选择。速度问题和工具维护停滞是两个根本性障碍。

---

## 2. QuantStats 评估

### 2.1 关键澄清：QuantStats 不是回测引擎

**QuantStats 是一个分析报告库，不是回测引擎。**

它只接受一条已经计算好的 `pd.Series` 收益率序列，生成可视化报告。它无法：
- 模拟交易执行
- 管理持仓状态
- 生成任何交易信号

这个工具在整个架构里的位置是**层级 4（报告层）**，而不是层级 1。评估它是否适合"实现策略"是错误的问题，正确的问题是：它是否是最好的报告工具。

### 2.2 作为报告层的能力评估

**能生成的报告（开箱即用）：**
- 全周期净值曲线（vs SPY 基准）
- 水下回撤图（Underwater Plot）
- 月度收益热力图（12 × N 年矩阵）
- Sharpe、Sortino、Calmar、Max DD、Win Rate 等核心指标
- Rolling Sharpe（可配置窗口）

**不能生成的（需要自己写）：**
- 参数敏感性热力图（这是回测层的输出，不是报告层）
- 逐笔交易明细（R 倍数分布、gap_adjusted_loss_multiple 分析）
- Monte Carlo 路径图（1000 条净值曲线 band）
- Regime decomposition（按市场环境分段绩效）
- Execution Fragility Map

**工具状态：**
- 原始 `quantstats` 仓库维护停滞（最后更新 2023）
- 推荐使用 `quantstats-lumi` 分支（更活跃的维护版本，功能兼容）

### 2.3 综合评分

| 维度 | 评分 | 说明 |
|---|---|---|
| 作为报告层的能力 | ★★★★☆ | 覆盖基础报告需求，专业美观 |
| 覆盖本策略全部报告需求 | ★★★☆☆ | 需要大量自定义补充（MC 图、Regime 分析等） |
| 与任何引擎的兼容性 | ★★★★★ | 只需要 returns Series，完全解耦 |
| **综合** | **★★★★☆** | **适合作为报告层，但需要补充自定义图表** |

**结论：** 使用 `quantstats-lumi`，覆盖标准报告部分。本策略设计的分析深度（MC 路径图、Regime 分析、Execution Fragility Map）需要额外自定义代码，QuantStats 只是起点。

---

## 3. vectorbt 评估

### 3.1 基本信息

| 项目 | 状态 |
|---|---|
| 定位 | 向量化回测框架，核心优势是速度 |
| 维护状态 | OSS 版本活跃（vectorbt），商业版 vectorbt Pro 更强 |
| 速度 | 比 Backtrader 快 10–100 倍（取决于策略复杂度） |
| 设计哲学 | 用 NumPy 广播机制同时跑成千上万个参数组合 |

### 3.2 核心矛盾：向量化 vs 状态机

vectorbt 的速度来源于**向量化**：所有计算在 NumPy 矩阵层面同时完成，没有 Python for 循环。

但本策略的核心逻辑是**顺序依赖的状态机**：

```
问题 1：R-multiple 分段 trailing stop
  ─ R < 1R：trail = highest_high - 2×ATR
  ─ 1R ≤ R < 3R：trail = highest_high - 3×ATR
  ─ R ≥ 3R：trail = highest_high - 5×ATR

  这三个阶段的切换依赖于"当前价格 vs 入场价 vs 初始止损"
  这是每个持仓独立的、路径依赖的计算
  → 无法向量化
```

```
问题 2：实时相关性过滤
  ─ 信号 A 处理完，更新持仓列表
  ─ 信号 B 处理时，需要用已更新的持仓列表算相关性
  ─ 信号 C 处理时，再次用已更新的持仓列表...

  这是严格的顺序依赖
  → 在 vectorbt 的矩阵模型里无法表达
```

```
问题 3：heat limit 检查
  ─ 每处理一个信号，更新 existing_risk
  ─ 下一个信号用新的 existing_risk 判断 heat
  
  → 同上，顺序依赖
```

**尝试在 vectorbt 里"近似实现"的后果：**
- 要么忽略相关性过滤 → 结果偏乐观（更多交易进入，相关仓位未被压制）
- 要么忽略 heat limit 的顺序更新 → 结果不准确
- 要么用 Python 循环实现状态机，速度退化回 Backtrader 水平，同时失去 vectorbt 的调试友好性

### 3.3 vectorbt 真正适合的场景

在本策略的工作流里，vectorbt 有一个合法的使用场景：**简化参数预筛选**。

具体做法：

```
使用 vectorbt 的简化版策略（去掉相关性过滤和顺序信号处理）
↓
快速扫描 N × ATR_multiplier × Trailing_multiplier 的大范围参数空间
↓
找出大致的"平坦区间"（哪些参数组合不崩）
↓
在筛出的候选参数上，用精确的主引擎重新验证
```

这个用法是合理的，但必须明确：**vectorbt 的结果只是方向性参考，不能作为最终回测结论。**

### 3.4 综合评分

| 维度 | 评分 | 说明 |
|---|---|---|
| 实现本策略完整逻辑的能力 | ★★☆☆☆ | 核心状态机逻辑无法正确向量化 |
| 简化参数预筛选的速度 | ★★★★★ | 这是它的强项 |
| look-ahead bias 风险 | ★★★☆☆ | 向量化计算容易引入，需要格外小心 |
| **综合（全功能使用）** | **★★☆☆☆** | **不适合作为主回测引擎** |
| **综合（仅用于参数预筛选）** | **★★★★☆** | **合适的辅助工具** |

**结论：** vectorbt 不能替代主回测引擎，但可以作为参数预筛选的加速器。

---

## 4. 三个工具综合对比

| 评估维度 | Backtrader | QuantStats | vectorbt |
|---|---|---|---|
| **工具定位** | 事件驱动回测引擎 | 报告分析库（非回测） | 向量化回测框架 |
| **实现策略完整逻辑** | ★★★★☆（慢） | N/A | ★★☆☆☆（不能正确实现） |
| **3000+ 标的回测速度** | ★★☆☆☆ | N/A | ★★☆☆☆（需简化） |
| **参数扫描能力** | ★★☆☆☆（笨重） | N/A | ★★★★★（向量化） |
| **报告输出** | ★★★☆☆（基础） | ★★★★☆ | ★★★☆☆（基础） |
| **工具维护状态** | ★★☆☆☆（停滞） | ★★★☆☆（缓慢） | ★★★★☆（活跃） |
| **调试难度** | 低（逐行 Python） | N/A | 高（矩阵索引追踪困难） |
| **推荐用途** | 不推荐 | 报告层 ✔ | 参数预筛选 ✔ |

---

## 5. 推荐方案：自定义事件循环引擎

### 5.1 核心结论

**三个工具都不适合作为本策略的主回测引擎。**

原因总结：
- Backtrader：速度不可接受，工具停滞
- vectorbt：无法正确实现状态机逻辑
- QuantStats：不是回测引擎

**推荐：自己写一个轻量级事件循环引擎（约 400–600 行 Python）。**

这在专业量化研究中是常见选择。当策略逻辑足够复杂，没有现成框架能完美匹配时，自写引擎反而是最干净、最可控的方案。

### 5.2 自定义引擎的架构

```
阶段 0：数据预计算（全部向量化，快）
  ─ 用 pandas/numpy 批量计算：ATR(20)、rolling_high_N、ADV_60、log_returns
  ─ 结果存成 dict of DataFrames，按 ticker 索引
  ─ 这一步跑一次，后续所有回测复用

阶段 1：事件循环（顺序执行，必须）
  for date in trading_dates:                         # 约 5500 次
    # 1. 更新持仓状态
    for pos in current_positions:
      update R_multiple, highest_high, trail_stop
    
    # 2. 生成平仓信号（止损 + 止盈）
    exits = check_exits(current_positions, data[date])
    
    # 3. 执行平仓，释放资金
    execute_exits(exits, data[date+1].open)
    
    # 4. 生成所有开仓信号，按 breakout_strength 排序
    signals = get_breakout_signals(data[date], N)
    signals.sort_by(breakout_strength)
    
    # 5. 顺序处理每个信号
    for signal in signals:
      correlation = compute_correlation(signal, current_positions, data)
      heat = compute_heat(current_positions, signal)
      if passes_filters(correlation, heat):
        execute_entry(signal, data[date+1].open)
        update_portfolio_state()
    
    # 6. 现金买入 SGOV
    manage_cash(SGOV_price[date+1])

阶段 2：绩效计算
  ─ 从逐笔 trade log 生成 daily returns Series
  ─ 喂给 QuantStats 生成报告
```

### 5.3 速度估算

```
数据预计算（一次性）：5–15 分钟（全量数据 pandas 运算）
单次回测（事件循环）：3–10 分钟（3000 只 × 5500 天，Python 循环）
参数扫描 100 组：
  ─ 4 核并行：约 1–4 小时
  ─ 比 Backtrader 快（因为数据预计算可以复用）
Monte Carlo 1000 路径：
  ─ 基于 returns bootstrap（不需要重跑回测）：< 5 分钟
```

单次回测 3–10 分钟是可接受的研究速度。

### 5.4 与三个工具的分工

```
工具分工（推荐）：

自定义事件循环引擎（主引擎）
  → Baseline 回测
  → Perturbation tests（每个参数扰动跑完整策略）
  → Walk-Forward OOS 验证
  → Execution stress tests

vectorbt（简化版，辅助预筛选）
  → 参数 heatmap 的大范围初筛
  → 只跑 N × ATR_multiplier（去掉相关性过滤和顺序处理）
  → 结论仅供方向参考，不作为最终依据

QuantStats（quantstats-lumi）
  → 所有回测的标准绩效报告
  → 净值曲线、回撤图、月度热力图

自定义 Python（统计分析）
  → Monte Carlo（return bootstrap + block bootstrap）
  → Regime decomposition
  → gap_adjusted_loss_multiple 分析
  → Execution Fragility Map
```

---

## 6. 替代方案参考

如果不想自写引擎，以下工具比 Backtrader 更适合：

| 工具 | 优点 | 缺点 | 适合场景 |
|---|---|---|---|
| **zipline-reloaded** | 成熟，支持大规模 universe，维护活跃 | 学习曲线较陡，配置复杂 | 有时间学习框架，追求工程规范 |
| **bt（backtesting library）** | 简单清晰，组合层设计合理 | 状态机实现不如自写灵活 | 策略逻辑相对简单时 |
| **Nautilus Trader** | 极快（Rust 内核），机构级设计 | 学习成本极高，文档较少 | 未来实盘接近时考虑 |
| **QuantConnect LEAN** | 数据+引擎一体，大规模 universe，支持 live | 学习成本高，本地部署复杂 | 时间充裕且目标实盘 |

**关于 QuantConnect LEAN**：设计文档里将其列为"未来实盘才考虑"，但实际上 LEAN 在研究阶段也值得考虑——它原生解决了幸存者偏差、Point-in-time 数据、大规模 universe 等问题，代价是学习成本较高。

---

## 7. 总结与决策建议

### 7.1 三个工具的最终定位

| 工具 | 最终定位 | 用还是不用 |
|---|---|---|
| **Backtrader** | 淘汰，不用作主引擎 | 不用 |
| **QuantStats** | 报告层，必要但非核心 | 用（推荐 quantstats-lumi） |
| **vectorbt** | 参数预筛选辅助 | 有条件地用，明确其局限性 |

### 7.2 推荐技术栈

```
主回测引擎：自定义 Python 事件循环
  依赖：pandas, numpy, scipy（相关性计算）
  代码量：400–600 行
  优势：完全正确、可调试、可控

参数预筛选：vectorbt（简化版策略）
  用途：参数 heatmap 大范围初筛
  注意：结果为近似，不可作为最终依据

报告层：quantstats-lumi
  用途：标准绩效报告
  补充：自定义 matplotlib 图表（MC、Regime、Fragility Map）

数据层：Polygon.io + Sharadar（按第4节数据规范）
```

### 7.3 行动优先级

```
优先级 1（立即开始）：
  ─ 搭建自定义回测引擎框架
  ─ 先用 Yahoo Finance 数据开发和调试
  ─ 验证策略逻辑的实现正确性（用少数标的 + 短时间段）

优先级 2（引擎验证后）：
  ─ 接入 Polygon.io 真实数据
  ─ 跑 Baseline 回测，得到初步净值曲线
  ─ 集成 quantstats-lumi 生成报告

优先级 3（Baseline 稳定后）：
  ─ 用 vectorbt 做参数 heatmap 初筛
  ─ 用自定义引擎跑 Perturbation tests
  ─ Monte Carlo + Walk-Forward
```
