# 趋势跟踪策略历史回测程序实现计划书

> 本文档对应设计方案 Session 1（趋势跟踪策略1.0）和 Session 1.2（策略1.0历史回测），
> 详细描述如何用 Yahoo Finance 数据实现完整的程序框架，
> 并保证代码高度模块化，未来可以平滑切换到商业数据源和策略2.0。

---

## 一、总体设计原则

### 1.1 两个核心约束

**约束A：数据源可切换**

Yahoo Finance 是开发阶段的过渡数据源，代码必须完全隔离数据源实现细节。
当未来切换到 Polygon.io / Norgate / Sharadar 时，只需要换一个 Adapter 类，
策略逻辑、回测引擎、分析模块一行代码都不需要修改。

**约束B：策略可扩展**

策略1.0 和策略2.0 的区别主要在 Entry 条件和部分 Exit 细节。
仓位管理、组合风险控制、分析框架是完全共用的。
设计时把"可能变化的部分"和"不会变化的部分"严格分开。

### 1.2 技术栈

| 组件 | 工具 | 理由 |
|---|---|---|
| 数据源（开发期） | yfinance | 免费，够用于原型 |
| 本地数据缓存 | Parquet 文件 + DuckDB | 快速读取，低依赖 |
| 回测引擎 | 自定义事件驱动引擎（Python） | 策略逻辑太复杂，Backtrader对多标的状态机支持较弱 |
| 向量化计算 | pandas / numpy | 指标计算 |
| 参数扫描 | 自定义 + multiprocessing | 并行回测多参数组合 |
| 性能分析报告 | QuantStats | 专业基金报告 |
| 可视化 | matplotlib / seaborn | 热力图、净值曲线 |

> **为什么不用 Backtrader？**
> Backtrader 对单标的策略很方便，但本策略是多标的组合，每根K线都需要在全市场扫描信号、管理几十个持仓的状态机（highest_high、trail_stop、stop_loss），并且做跨标的相关性计算。在 Backtrader 中实现这些逻辑，代码会非常绕，调试困难，multiprocessing 也很难做。用自定义引擎反而更清晰，速度也更快（向量化预处理 + 逐日事件循环只处理活跃持仓）。

---

## 二、项目目录结构

```
fund_trend_following/
│
├── data/                          # 数据层（完全隔离数据源）
│   ├── adapters/
│   │   ├── base.py               # 数据源抽象基类（接口定义）
│   │   ├── yahoo.py              # Yahoo Finance 实现
│   │   └── polygon.py            # 未来：Polygon.io 实现（留空）
│   ├── pipeline.py               # 数据管道：加载、清洗、对齐、缓存
│   ├── universe.py               # 标的池管理（过滤逻辑）
│   └── cache/                    # 本地 Parquet 缓存（不上传 git）
│       ├── prices/               # 按 ticker 分文件存储
│       └── universe/             # 缓存的 universe 快照
│
├── indicators/                    # 指标计算层（纯函数，无副作用）
│   ├── atr.py                    # ATR（Wilder's smoothing）
│   ├── breakout.py               # 滚动高点、突破信号
│   ├── adv.py                    # 日均成交金额
│   └── correlation.py            # 对数收益率相关性
│
├── strategy/                      # 策略逻辑层
│   ├── base.py                   # 策略基类（定义接口）
│   ├── params.py                 # 参数数据类（ParamSet）
│   ├── v1/
│   │   ├── entry.py              # 策略1.0 开仓逻辑
│   │   ├── exit.py               # 策略1.0 止损/止盈逻辑
│   │   ├── sizing.py             # 仓位计算（raw → capped → correlation → heat）
│   │   └── strategy_v1.py        # 策略1.0 组装（调用以上三个模块）
│   └── v2/
│       └── strategy_v2.py        # 未来：策略2.0（继承 base，覆盖 entry）
│
├── backtest/                      # 回测引擎层
│   ├── engine.py                 # 事件驱动主循环
│   ├── portfolio.py              # 组合状态（持仓、现金、NAV、heat）
│   ├── execution.py              # 交易执行模型（滑点、手续费、gap filter）
│   ├── position.py               # 单笔持仓状态机
│   └── results.py                # 回测结果容器（trades、daily_nav、metrics）
│
├── analysis/                      # 分析层（可独立于策略和回测引擎使用）
│   ├── metrics.py                # 核心绩效指标（CAGR、Sharpe、MaxDD 等）
│   ├── perturbation.py           # 1.2.2 扰动测试
│   ├── heatmap.py                # 1.2.5 参数热力图
│   ├── montecarlo.py             # 1.2.4 蒙特卡洛模拟
│   ├── walkforward.py            # 1.2.7 Walk-Forward OOS
│   ├── stress.py                 # 1.2.6 执行压力测试
│   ├── regime.py                 # 1.2.8 市场环境鲁棒性
│   └── diagnostics.py            # 1.2.3 交易诊断（gap_adjusted_loss_multiple）
│
├── reports/                       # 报告层
│   ├── generator.py              # QuantStats + 自定义报告生成
│   └── charts.py                 # 净值曲线、参数热力图、蒙特卡洛图
│
├── scripts/                       # 可执行脚本（命令行入口）
│   ├── 01_download_data.py       # 下载并缓存 Yahoo Finance 数据
│   ├── 02_run_baseline.py        # 1.2.1 Baseline 回测
│   ├── 03_run_perturbation.py    # 1.2.2 扰动测试
│   ├── 04_run_diagnostics.py     # 1.2.3 交易诊断
│   ├── 05_run_montecarlo.py      # 1.2.4 蒙特卡洛
│   ├── 06_run_heatmap.py         # 1.2.5 参数热力图
│   ├── 07_run_stress.py          # 1.2.6 执行压力测试
│   ├── 08_run_walkforward.py     # 1.2.7 Walk-Forward
│   └── 09_run_regime.py          # 1.2.8 Regime 分析
│
├── config/
│   ├── baseline_params.py        # 策略1.0 Baseline anchor 参数
│   └── perturbation_grid.py      # 扰动测试参数网格定义
│
├── results/                       # 回测输出（不上传 git）
│   ├── baseline/
│   ├── perturbation/
│   ├── heatmap/
│   ├── montecarlo/
│   ├── walkforward/
│   └── stress/
│
└── notebooks/
    └── exploration.ipynb          # 探索性分析（可选）
```

---

## 三、各模块详细实现说明

### 3.1 数据层（`data/`）

#### 3.1.1 数据源抽象基类 `data/adapters/base.py`

这是整个数据层的核心设计，定义所有数据源必须实现的接口：

```python
from abc import ABC, abstractmethod
import pandas as pd

class DataSourceAdapter(ABC):
    """
    数据源抽象基类。
    所有数据源（Yahoo、Polygon、Norgate）必须实现此接口。
    策略/回测代码只依赖此接口，不依赖任何具体数据源。
    """

    @abstractmethod
    def get_ohlcv(
        self,
        ticker: str,
        start: str,
        end: str,
    ) -> pd.DataFrame:
        """
        返回指定标的的调整后日线数据。

        返回 DataFrame 必须包含以下列（全部为复权后价格）：
            date        : datetime index
            open        : 复权开盘价
            high        : 复权最高价
            low         : 复权最低价
            close       : 复权收盘价
            volume      : 成交量（股数）
            dollar_vol  : 成交金额（= close × volume，用于 ADV 计算）
        """
        pass

    @abstractmethod
    def get_universe(
        self,
        date: str,
        min_price: float,
        min_market_cap_b: float,
        min_adv_m: float,
    ) -> list[str]:
        """
        返回指定日期满足条件的标的列表。
        注意：必须是 point-in-time，不得包含该日期之后才存在的标的。
        """
        pass

    @abstractmethod
    def get_market_cap(self, ticker: str, date: str) -> float:
        """
        返回指定标的在指定日期的市值（单位：美元）。
        必须是 point-in-time 数据。
        """
        pass
```

**切换数据源只需要做这一件事**：
把 `engine.py` 里的 `adapter = YahooFinanceAdapter()` 换成 `adapter = PolygonAdapter()`，
其余所有代码保持不变。

---

#### 3.1.2 Yahoo Finance Adapter `data/adapters/yahoo.py`

Yahoo Finance 的核心问题和解决方案：

**问题1：Yahoo 没有 adjusted open/high/low**

解决方案：用 `adj_factor = adj_close / close` 自己推算：

```python
adj_factor = df['adj_close'] / df['close']
df['open']  = df['open']  * adj_factor
df['high']  = df['high']  * adj_factor
df['low']   = df['low']   * adj_factor
df['close'] = df['adj_close']
df['dollar_vol'] = df['close'] * df['volume']
```

**问题2：Yahoo 没有动态市值（point-in-time）**

解决方案：
- 使用 `yfinance.Ticker.info['marketCap']` 获取当前市值作为静态代理
- 在策略1.0原型阶段，接受这个近似值（在计划书末尾说明其对结果的影响）
- 商业数据源切换后，此限制自动消除

**问题3：Yahoo 无法过滤退市股**

解决方案：
- 使用两个公开的历史成分股列表作为 universe：
  - S&P 500 历史成分股（Wikipedia 可获取，或使用 `pandas-datareader`）
  - Russell 1000/2000 历史成分股（部分可通过 CRSP 代理获取）
- 在策略原型阶段，明确标注"存在幸存者偏差，结果偏乐观"
- 商业数据源切换后，此限制自动消除

**问题4：Yahoo 开盘价偶发脏数据**

解决方案：数据清洗（见 `pipeline.py`）：
- 若 `open > high` 或 `open < low`，将 open 替换为 `(high + low) / 2`
- 若某日 `abs(adj_factor - 1) > 0.5`（即单日调整因子超过50%），标记为可疑数据，跳过该日信号生成

---

#### 3.1.3 数据管道 `data/pipeline.py`

职责：批量加载、清洗、缓存数据，供回测引擎使用。

**主要函数：**

```python
def load_price_data(
    tickers: list[str],
    start: str,
    end: str,
    adapter: DataSourceAdapter,
    use_cache: bool = True,
) -> dict[str, pd.DataFrame]:
    """
    批量加载价格数据。
    先查本地 Parquet 缓存，缺失的再从 adapter 下载。
    返回：{ticker: DataFrame} 的字典
    """

def clean_ohlcv(df: pd.DataFrame) -> pd.DataFrame:
    """
    数据清洗：
    1. 删除 volume = 0 的行（停牌日，标记为不可交易）
    2. 修正 open > high 或 open < low 的脏数据
    3. 检测并标记异常价格跳动（adj_factor 超过50%的日期）
    4. 添加 is_tradable 列（True/False）
    """

def build_panel(
    price_data: dict[str, pd.DataFrame],
    start: str,
    end: str,
) -> dict[str, pd.DataFrame]:
    """
    构建全市场面板数据（对齐时间索引）。
    不存在的日期不做前向填充，保留 NaN，is_tradable = False。
    """
```

**缓存策略：**
- 每个 ticker 保存为独立的 Parquet 文件：`cache/prices/{ticker}.parquet`
- 增量更新：只下载缓存中缺失的日期范围
- 缓存版本控制：文件名中加入 `adj_factor` 的 hash，确保复权因子变化时自动失效

---

#### 3.1.4 标的池管理 `data/universe.py`

```python
def get_universe_at_date(
    date: pd.Timestamp,
    price_panel: dict[str, pd.DataFrame],
    min_price: float = 10.0,
    min_market_cap_b: float = 20.0,
    min_adv_m: float = 20.0,
    adapter: DataSourceAdapter = None,
) -> list[str]:
    """
    返回在指定日期满足所有过滤条件的标的列表。
    
    过滤条件：
    1. is_tradable[date] = True（该日有有效数据）
    2. close[date] > min_price
    3. ADV_60[date] > min_adv_m × 1_000_000
       （ADV_60 使用截至 date 前60个交易日的平均成交金额，不含当日）
    4. market_cap[date] > min_market_cap_b × 1_000_000_000
    
    注意：ADV_60 使用 date 之前的数据（shift=1），避免 look-ahead bias
    """
```

---

### 3.2 指标计算层（`indicators/`）

所有指标计算函数都是**纯函数**（给定输入，返回确定输出，无副作用）。
这保证了指标计算可以被任何策略版本复用。

#### 3.2.1 ATR计算 `indicators/atr.py`

实现 Wilder's smoothing method，严格按设计方案公式：

```python
def compute_atr(
    high: pd.Series,
    low: pd.Series,
    close: pd.Series,
    period: int = 20,
) -> pd.Series:
    """
    计算 ATR（Average True Range），使用 Wilder's smoothing。

    TR[i] = max(
        high[i] - low[i],
        abs(high[i] - close[i-1]),
        abs(low[i] - close[i-1])
    )

    ATR 初始值 = 前 period 个 TR 的简单平均
    ATR[i] = (ATR[i-1] × (period-1) + TR[i]) / period

    返回与输入等长的 Series，前 period-1 行为 NaN。
    """
```

**关键细节**：ATR 必须用全历史数据递推计算到 t 日，不是滚动窗口。
这意味着 2004 年的 ATR 要从该 ticker 有数据的第一天开始递推。

---

#### 3.2.2 突破信号 `indicators/breakout.py`

```python
def compute_rolling_high(
    high: pd.Series,
    window: int = 100,
) -> pd.Series:
    """
    rolling_high_N[t] = max(high[t-N : t-1])
    即过去 N 根K线（不含当日）的最高价。
    实现：high.shift(1).rolling(window).max()
    """

def compute_breakout_signal(
    close: pd.Series,
    rolling_high: pd.Series,
) -> pd.Series:
    """
    breakout_signal[t] = close[t] > rolling_high[t]
    返回 bool Series。
    """

def compute_breakout_strength(
    close: pd.Series,
    rolling_high: pd.Series,
) -> pd.Series:
    """
    breakout_strength[t] = close[t] / rolling_high[t]
    用于多信号排序（降序）。
    """
```

---

#### 3.2.3 ADV计算 `indicators/adv.py`

```python
def compute_adv(
    dollar_vol: pd.Series,
    window: int = 60,
) -> pd.Series:
    """
    ADV_60[t] = mean(dollar_vol[t-60 : t-1])
    注意：不含当日（shift=1），避免 look-ahead bias。
    实现：dollar_vol.shift(1).rolling(window).mean()
    """
```

---

#### 3.2.4 相关性计算 `indicators/correlation.py`

```python
def compute_pairwise_correlation(
    returns_new: pd.Series,
    returns_existing: pd.Series,
    window: int = 60,
    min_samples: int = 40,
) -> float | None:
    """
    计算新开仓标的与已持仓标的之间的 Pearson 相关系数。

    实现步骤（严格按设计方案）：
    1. 对齐两个序列（inner join on date index）
    2. 取最近 window 个观测值
    3. 若样本数 < min_samples，返回 None
    4. 若任一序列标准差为 0，返回 None
    5. 计算 Pearson correlation

    输入为对数收益率序列（log returns）。
    """

def compute_max_correlation(
    new_ticker: str,
    current_positions: list[str],
    log_returns: dict[str, pd.Series],
    as_of_date: pd.Timestamp,
    window: int = 60,
    min_samples: int = 40,
) -> float:
    """
    计算新标的与所有当前持仓的最大相关性。

    返回 max(abs(corr)) across all positions。
    若无有效相关性计算结果，返回 0.0（按设计方案：视为无相关性）。
    """
```

---

### 3.3 策略层（`strategy/`）

#### 3.3.1 参数类 `strategy/params.py`

用 Python `dataclass` 定义参数，方便序列化和批量测试：

```python
from dataclasses import dataclass, field

@dataclass
class StrategyParams:
    # === Universe 过滤 ===
    min_price: float = 10.0           # USD
    min_market_cap_b: float = 20.0    # 亿美元
    min_adv_m: float = 20.0           # 百万美元

    # === 开仓信号 ===
    breakout_window: int = 100         # 突破窗口 N

    # === ATR ===
    atr_period: int = 20

    # === 止损 ===
    stop_loss_multiplier: float = 2.0
    min_stop_distance_pct: float = 0.005  # 0.5%

    # === 止盈（Trailing Stop）===
    trail_multiplier_r1: float = 2.0   # R_multiple < 1 时
    trail_multiplier_r3: float = 3.0   # 1 <= R_multiple < 3 时
    trail_multiplier_r5: float = 5.0   # R_multiple >= 3 时

    # === 仓位管理 ===
    risk_per_trade: float = 0.01       # 1% NAV
    position_cap: float = 0.05         # 5% NAV

    # === 组合风险 ===
    heat_limit: float = 0.10           # 10%

    # === 相关性 ===
    correlation_window: int = 60
    correlation_threshold: float = 0.7
    correlation_reduction: float = 0.5  # 超过阈值时的仓位缩减比例

    # === 执行模型 ===
    gap_filter: float = 0.025          # 2.5%
    commission_bps: float = 3.0        # 3 bps
    slippage_bps: float = 10.0         # 10 bps

    # === 空仓资金 ===
    cash_proxy: str = "SGOV"           # 空仓资金投入的标的
```

**这个 ParamSet 是所有测试的核心**。Baseline 用默认值，扰动测试只改一个字段，
Walk-Forward 每个 IS 窗口生成一个 ParamSet，参数扫描批量生成多个 ParamSet。

---

#### 3.3.2 策略基类 `strategy/base.py`

定义策略接口，供 V1 和 V2 继承：

```python
from abc import ABC, abstractmethod

class BaseStrategy(ABC):
    def __init__(self, params: StrategyParams):
        self.params = params

    @abstractmethod
    def generate_entry_signals(
        self,
        date: pd.Timestamp,
        universe: list[str],
        price_panel: dict[str, pd.DataFrame],
        indicators: dict[str, dict],  # {ticker: {atr, rolling_high, adv, ...}}
        portfolio: 'Portfolio',
    ) -> list[dict]:
        """
        生成当日（t日收盘后）的开仓信号列表。
        每个信号包含：ticker, breakout_strength, 止损价, 仓位等
        返回按 breakout_strength 降序排列的信号列表。
        """
        pass

    @abstractmethod
    def generate_exit_signals(
        self,
        date: pd.Timestamp,
        portfolio: 'Portfolio',
        price_panel: dict[str, pd.DataFrame],
        indicators: dict[str, dict],
    ) -> list[dict]:
        """
        生成当日（t日收盘后）的平仓信号列表。
        包含：stop_loss 触发、trailing_stop 触发。
        """
        pass
```

---

#### 3.3.3 策略1.0开仓逻辑 `strategy/v1/entry.py`

```python
def generate_entry_signals_v1(
    date: pd.Timestamp,
    universe: list[str],
    price_panel: dict[str, pd.DataFrame],
    indicators: dict,
    portfolio: 'Portfolio',
    params: StrategyParams,
    log_returns: dict[str, pd.Series],
) -> list[dict]:
    """
    完整实现设计方案 Section A（开仓条件）和 Section B（开仓执行预处理）。

    具体步骤：
    1. 遍历 universe，对每个 ticker 检查：
       a. ADV_60[date] > params.min_adv_m × 1e6
       b. close[date] > params.min_price
       c. 动态市值 > params.min_market_cap_b × 1e9
       d. breakout_signal[date] = True（close > rolling_high_N）
       e. 该 ticker 当前未持仓
    
    2. 对满足条件的 ticker 计算：
       a. 初步止损价：entry_price_estimate - params.stop_loss_multiplier × ATR[date]
          （此时用 close[date] 作为 entry_price 的估计值，实际 entry_price 在 t+1 开盘确定）
       b. stop_distance = (close[date] - stop_loss) / close[date]
       c. 若 stop_distance < params.min_stop_distance_pct → 过滤掉
       d. 计算 breakout_strength = close[date] / rolling_high[date]
    
    3. 按 breakout_strength 降序排序
    
    4. 返回信号列表（注意：相关性检查和 heat 检查在回测引擎中逐个执行）

    注意：entry_price 和最终止损价在 t+1 开盘后才能确定，
    这里只做信号筛选，不做最终仓位计算。
    """
```

**关键设计**：开仓信号生成和开仓执行是两个步骤，分别在 `entry.py` 和 `engine.py` 中。
这是因为 gap filter、相关性检查、heat 检查都依赖 t+1 的开盘价或实时组合状态。

---

#### 3.3.4 策略1.0止损/止盈逻辑 `strategy/v1/exit.py`

```python
def check_exit_signals_v1(
    date: pd.Timestamp,
    position: 'Position',
    high: float,
    low: float,
    close: float,
    atr: float,
) -> dict | None:
    """
    在 t 日收盘后，对单个持仓检查是否触发平仓信号。

    检查顺序（优先级从高到低）：
    1. Stop Loss：若 low[date] < position.stop_loss → 触发止损
    2. Trailing Stop：若 close[date] < position.trail_stop → 触发移动止损

    同时更新持仓状态：
    - position.highest_high = max(position.highest_high, high[date])
    - 计算 R_multiple = (close[date] - position.entry_price) / position.R
    - 根据 R_multiple 更新 trail_stop（按设计方案的分段公式）

    返回：
    - None：无平仓信号
    - {'ticker': ..., 'reason': 'stop_loss'|'trailing_stop'} ：平仓信号
    """

def update_trail_stop_v1(
    position: 'Position',
    highest_high: float,
    atr: float,
    params: StrategyParams,
) -> float:
    """
    按设计方案 Section D 的分段公式更新 trail_stop。
    
    if R_multiple < 1: multiplier = params.trail_multiplier_r1 (2.0)
    elif R_multiple < 3: multiplier = params.trail_multiplier_r3 (3.0)
    else: multiplier = params.trail_multiplier_r5 (5.0)
    
    new_trail = highest_high - multiplier × ATR
    trail_stop = max(position.trail_stop, new_trail)
    （trail_stop 只能上移，不能下移）
    """
```

---

#### 3.3.5 仓位计算 `strategy/v1/sizing.py`

```python
def compute_position_size(
    entry_price: float,
    stop_loss: float,
    nav: float,
    existing_risk: float,
    current_positions: list[str],
    log_returns: dict[str, pd.Series],
    new_ticker: str,
    as_of_date: pd.Timestamp,
    params: StrategyParams,
) -> int | None:
    """
    完整实现设计方案 Section B 的仓位计算流程。

    Step 1: 计算原始仓位
        risk_amount = nav × params.risk_per_trade
        raw_position = risk_amount / (entry_price - stop_loss)

    Step 2: 单标的上限
        capped_position = (nav × params.position_cap) / entry_price
        preliminary_position = min(raw_position, capped_position)

    Step 3: 相关性检查
        max_corr = compute_max_correlation(new_ticker, current_positions, ...)
        if max_corr > params.correlation_threshold:
            final_position = preliminary_position × params.correlation_reduction
        else:
            final_position = preliminary_position

    Step 4: 组合 heat 检查
        new_risk = final_position × (entry_price - stop_loss)
        total_heat = (existing_risk + new_risk) / nav
        if total_heat > params.heat_limit:
            return None  # 放弃该信号

    Step 5: 向下取整到整数股数
        return int(final_position)  # 最少1股，否则放弃

    返回：int（股数）或 None（放弃开仓）
    """
```

---

### 3.4 回测引擎层（`backtest/`）

#### 3.4.1 持仓状态机 `backtest/position.py`

```python
@dataclass
class Position:
    """
    单笔持仓的完整状态。
    这是策略最核心的状态单元，每天都需要更新。
    """
    ticker: str
    entry_date: pd.Timestamp
    entry_price: float
    shares: int
    stop_loss: float           # 固定止损价（不上移）
    trail_stop: float          # 移动止盈止损价（只上移）
    highest_high: float        # 入场后的最高价（初始化为 entry_price）
    atr_at_entry: float        # 入场时的 ATR（用于计算 R）

    @property
    def R(self) -> float:
        """初始风险单位 = entry_price - stop_loss"""
        return self.entry_price - self.stop_loss

    @property
    def market_value(self, current_price: float) -> float:
        return self.shares * current_price

    @property
    def risk_amount(self) -> float:
        """当前仓位的风险金额（用于 heat 计算）"""
        return self.shares * (self.entry_price - self.stop_loss)
```

---

#### 3.4.2 组合状态 `backtest/portfolio.py`

```python
class Portfolio:
    """
    管理整个投资组合的状态。
    每个交易日结束后更新。
    """
    def __init__(self, initial_capital: float):
        self.cash: float = initial_capital
        self.positions: dict[str, Position] = {}  # ticker → Position
        self.nav_history: list[tuple] = []         # (date, nav)
        self.trade_log: list[dict] = []            # 每笔成交记录

    @property
    def existing_risk(self) -> float:
        """所有持仓的风险金额之和（用于 heat 检查）"""
        return sum(p.risk_amount for p in self.positions.values())

    @property
    def nav(self) -> float:
        """当前总资产 = 现金 + 持仓市值"""
        # 需要当前价格，由引擎在每日结束时更新
        ...

    def open_position(self, position: Position, cost: float):
        """记录开仓，扣减现金（含手续费和滑点）"""

    def close_position(self, ticker: str, exit_price: float, reason: str, cost: float):
        """记录平仓，增加现金，写入 trade_log"""

    def update_nav(self, date: pd.Timestamp, price_panel: dict):
        """每日收盘后更新 NAV，记录到 nav_history"""
```

---

#### 3.4.3 交易执行模型 `backtest/execution.py`

```python
def apply_gap_filter(
    signal_close: float,
    next_open: float,
    gap_filter: float = 0.025,
) -> bool:
    """
    检查 t+1 开盘价是否在允许范围内。
    若 next_open > signal_close × (1 + gap_filter) 或
       next_open < signal_close × (1 - gap_filter)，
    返回 False（过滤掉该信号）。
    """

def compute_entry_price(
    next_open: float,
    direction: str = 'buy',
    slippage_bps: float = 10.0,
) -> float:
    """
    实际成交价 = 开盘价 + 滑点
    buy:  entry_price = next_open × (1 + slippage_bps / 10000)
    sell: exit_price  = next_open × (1 - slippage_bps / 10000)
    """

def compute_transaction_cost(
    entry_price: float,
    exit_price: float,
    shares: int,
    commission_bps: float = 3.0,
    slippage_bps: float = 10.0,
) -> float:
    """
    按设计方案 Section G：
    buy_cost  = entry_price × shares × (commission_bps + slippage_bps) / 10000
    sell_cost = exit_price  × shares × (commission_bps + slippage_bps) / 10000
    transaction_cost = buy_cost + sell_cost
    
    注意：这里的 entry_price/exit_price 已经是含滑点的成交价，
    commission_bps 是额外的佣金，slippage_bps 在这里代表市场冲击（已内建在价格中）。
    实际上按设计方案，commission = 3 bps + slippage = 10 bps，合计 13 bps，
    分别应用于买入和卖出。
    """
```

---

#### 3.4.4 主回测引擎 `backtest/engine.py`

这是整个系统的主控制器，实现每日事件循环：

```python
class BacktestEngine:
    def __init__(
        self,
        strategy: BaseStrategy,
        price_panel: dict[str, pd.DataFrame],
        indicators: dict[str, dict],  # 预计算好的所有指标
        params: StrategyParams,
        initial_capital: float = 10_000_000,  # $1000万
        adapter: DataSourceAdapter = None,
    ):
        self.strategy = strategy
        self.price_panel = price_panel
        self.indicators = indicators
        self.params = params
        self.portfolio = Portfolio(initial_capital)

    def run(
        self,
        start: str,
        end: str,
    ) -> 'BacktestResults':
        """
        主回测循环（逐日事件驱动）。
        
        每个交易日 t 的处理顺序：
        
        === t 日收盘后（使用 t 日数据）===
        Step 1: 更新所有持仓状态
            - 更新 highest_high[t] = max(highest_high[t-1], high[t])
            - 更新 trail_stop（分段公式）
        
        Step 2: 生成平仓信号
            - 检查每个持仓是否触发 stop_loss 或 trailing_stop
            - 按优先级排序（stop_loss > trailing_stop）
        
        Step 3: 生成开仓信号
            - 扫描 universe，找到突破信号
            - 按 breakout_strength 降序排序
        
        === t+1 日开盘时（使用 t+1 开盘价）===
        Step 4: 执行平仓指令
            - 对每个平仓信号，用 open[t+1] 成交
            - 更新 portfolio（现金 += 平仓收入 - 手续费）
        
        Step 5: 执行开仓指令（逐个处理）
            - 对每个开仓信号：
              a. Gap filter 检查（是否跳空过大）
              b. 确定实际 entry_price = open[t+1] × (1 + slippage)
              c. 重新计算止损价（基于实际 entry_price）
              d. 计算最终仓位（含相关性检查和 heat 检查）
              e. 若通过，执行开仓，更新 portfolio
              f. 更新 existing_risk（每笔后立即更新）
        
        Step 6: 空仓资金处理
            - 将剩余现金记账为 SGOV 持仓（按当日 SGOV 收益更新）
        
        Step 7: 更新每日 NAV
            - NAV = 现金 + 所有持仓市值（用收盘价）+ SGOV 市值
        
        返回 BacktestResults 对象
        """
```

**性能优化策略**：
- 所有指标（ATR、rolling high、ADV、log returns）在回测开始前**批量预计算**
- 每日循环只做状态更新和信号生成，不做重复计算
- 对于有大量参数组合的测试，使用 `multiprocessing.Pool` 并行运行多个 `BacktestEngine`

---

#### 3.4.5 回测结果容器 `backtest/results.py`

```python
@dataclass
class BacktestResults:
    params: StrategyParams
    daily_nav: pd.Series        # index=date, value=NAV（绝对值）
    daily_returns: pd.Series    # 每日收益率
    trade_log: pd.DataFrame     # 每笔交易明细（见下面字段说明）
    # trade_log 字段：
    # ticker, entry_date, entry_price, exit_date, exit_price, shares,
    # stop_loss, atr_at_entry, R, pnl, pnl_r_multiple, exit_reason,
    # holding_days, gap_adjusted_loss_multiple

    def compute_metrics(self, benchmark_returns: pd.Series = None) -> dict:
        """
        计算并返回所有核心绩效指标（调用 analysis/metrics.py）。
        """

    def to_dict(self) -> dict:
        """序列化为字典（用于并行回测结果收集）"""
```

---

### 3.5 分析层（`analysis/`）

分析层完全独立于回测引擎。所有分析函数只依赖 `BacktestResults`，不依赖策略实现。

#### 3.5.1 核心绩效指标 `analysis/metrics.py`

```python
def compute_metrics(
    daily_returns: pd.Series,
    trade_log: pd.DataFrame,
    benchmark_returns: pd.Series = None,
    risk_free_rate: float = 0.05,
) -> dict:
    """
    计算设计方案 1.2.1.2 中要求的全部核心绩效指标：

    收益类：
    - CAGR = (final_nav / initial_nav) ^ (252 / trading_days) - 1
    - Total Return

    风险类：
    - Max Drawdown：从峰值到谷底的最大回撤
    - Max Drawdown Duration：最长回撤持续时间（天）
    - Annual Volatility = std(daily_returns) × sqrt(252)

    风险回报类：
    - Sharpe = (mean(daily_returns) × 252 - rf) / (std(daily_returns) × sqrt(252))
    - Sortino（只用下行波动率）
    - Calmar = CAGR / abs(Max Drawdown)

    交易统计类：
    - Win Rate = 盈利交易数 / 总交易数
    - Avg Win / Avg Loss（R倍数）
    - Avg Holding Days
    - Num Trades per Year
    - Profit Factor = sum(wins) / sum(losses)

    组合特征类：
    - Turnover = 年均换手率（总交易名义金额 / 平均 NAV）
    - Exposure = 平均每日（持仓市值 / NAV）（有多少时间真正在市场里）
    """
```

---

#### 3.5.2 扰动测试 `analysis/perturbation.py`（对应1.2.2）

```python
def run_perturbation_test(
    param_name: str,           # 被扰动的参数名，如 'breakout_window'
    param_values: list,        # 测试的参数值列表，如 [80, 90, 100, 110, 120]
    baseline_params: StrategyParams,
    price_panel: dict,
    indicators: dict,
    start: str,
    end: str,
    n_workers: int = 4,        # 并行进程数
) -> pd.DataFrame:
    """
    逐参数扰动测试（one-at-a-time）。
    其他参数固定在 baseline_params，只改 param_name。

    返回：
        DataFrame，行 = param_values，列 = [CAGR, Sharpe, MaxDD, ...]
    
    同时计算：
    - Sensitivity = std(metric) / mean(metric) 对每个指标
    - Stability Region：找出 metric > baseline × 0.9 的连续参数区间
    """

def compute_sensitivity_cv(results_df: pd.DataFrame) -> pd.Series:
    """
    计算每个指标的 Coefficient of Variation（CV = std/mean）。
    CV < 0.1：稳健（参数变化 < ±10%）
    CV > 0.3：脆弱（参数变化 > ±30%）
    """

def find_stability_region(
    param_values: list,
    metric_values: pd.Series,
    threshold_pct: float = 0.9,  # baseline的90%以上视为稳定
) -> tuple[float, float]:
    """
    找出参数的稳定区间（连续的满足阈值的参数范围）。
    返回 (min_value, max_value)。
    """
```

---

#### 3.5.3 交易诊断 `analysis/diagnostics.py`（对应1.2.3）

```python
def compute_gap_adjusted_loss_multiple(
    trade_log: pd.DataFrame,
) -> pd.Series:
    """
    计算每笔交易的 gap_adjusted_loss_multiple。

    gap_adjusted_loss_multiple = (realized_exit_price - entry_price) / R
    其中 R = entry_price - stop_loss，realized_exit_price = actual fill price

    这个指标反映实际止损时的真实损失（含跳空和滑点）是否符合预期。
    
    理想情况：均值接近 -1.0（损失1倍R）
    危险信号：95th percentile 远超 -2.5R，max 超过 -10R
    """

def print_loss_multiple_report(trade_log: pd.DataFrame):
    """
    输出：
    - 均值、中位数
    - 5th / 25th / 75th / 95th percentile
    - 最大值（最差一笔止损）
    - 分布直方图
    """
```

---

#### 3.5.4 蒙特卡洛模拟 `analysis/montecarlo.py`（对应1.2.4）

```python
def run_return_bootstrap(
    daily_returns: pd.Series,
    n_simulations: int = 1000,
    seed: int = 42,
) -> pd.DataFrame:
    """
    Return Bootstrap（日收益率有放回重采样）。
    
    每次模拟：对 daily_returns 序列有放回地重采样，保持长度不变。
    不使用 trade-level shuffle（设计方案明确禁止）。
    
    返回：shape=(n_trading_days, n_simulations) 的 DataFrame，
    每列为一条模拟收益率路径。
    """

def run_block_bootstrap(
    daily_returns: pd.Series,
    block_size: str = 'monthly',  # 'monthly' or 'quarterly'
    n_simulations: int = 1000,
    seed: int = 42,
) -> pd.DataFrame:
    """
    Block Bootstrap（按月/季度分块随机重排）。
    
    保留块内的短期自相关结构，比 return bootstrap 更接近真实市场特性。
    
    步骤：
    1. 将收益率序列按月（或季度）切割成块
    2. 对块进行有放回随机重采样
    3. 拼接成新的收益率序列
    """

def compute_nav_paths(
    return_paths: pd.DataFrame,
    initial_nav: float = 1.0,
) -> pd.DataFrame:
    """从模拟收益率路径计算净值曲线。"""

def compute_max_drawdowns(nav_paths: pd.DataFrame) -> pd.Series:
    """计算每条路径的最大回撤。"""

def compute_drawdown_durations(nav_paths: pd.DataFrame) -> pd.DataFrame:
    """
    计算每条路径的水下时间分布（drawdown path dependency）。
    
    输出：
    - 连续亏损期 > 3/6/12/24 个月的概率
    - 平均水下时长
    - 95th percentile 水下时长
    - 最长水下时长
    """

def run_montecarlo(
    daily_returns: pd.Series,
    n_simulations: int = 1000,
    initial_nav: float = 10_000_000,
) -> dict:
    """
    完整蒙特卡洛分析，返回设计方案 1.2.4.2 要求的所有 Deliverables：
    
    - nav_paths：1000 条净值曲线
    - max_dd_distribution：最大回撤分布（median, 95th, worst）
    - cagr_distribution：CAGR分布（mean, std, 10th percentile）
    - sharpe_distribution：Sharpe分布（mean, std, skew）
    - drawdown_duration_table：连续亏损长度分布表
    """
```

---

#### 3.5.5 参数热力图 `analysis/heatmap.py`（对应1.2.5）

```python
def run_1d_heatmap(
    param_name: str,
    param_values: list,
    baseline_params: StrategyParams,
    price_panel: dict,
    indicators: dict,
    start: str,
    end: str,   # 注意：必须是IS区间，不得包含OOS数据
    metrics: list[str] = ['CAGR', 'Sharpe', 'MaxDD'],
) -> pd.DataFrame:
    """
    1D 参数热力图（单参数扫描）。
    用于：Breakout window N、ATR length、Stop loss multiplier、
         Trailing stop multiplier、Min stop distance。
    
    返回：每个参数值对应的绩效指标 DataFrame。
    """

def run_2d_heatmap(
    param1_name: str,
    param1_values: list,
    param2_name: str,
    param2_values: list,
    baseline_params: StrategyParams,
    price_panel: dict,
    indicators: dict,
    start: str,
    end: str,   # 严格只用IS数据
    metric: str = 'Sharpe',
) -> pd.DataFrame:
    """
    2D 参数热力图（双参数交叉扫描）。
    用于：N × ATR stop loss multiplier（必须），N × Trailing stop multiplier（可选）。
    
    返回：shape=(len(param1_values), len(param2_values)) 的 DataFrame，
    每个格子为对应参数组合的 Sharpe（或其他指标）。
    """

def run_slice_test(
    slice_param_name: str,
    slice_param_values: list,
    fixed_params: StrategyParams,  # N=100, ATR=20 固定
    price_panel: dict,
    indicators: dict,
    start: str,
    end: str,
) -> pd.DataFrame:
    """
    Slice Test（固定主参数，扫描次要参数）。
    用于：Correlation threshold、Risk per trade、Market cap filter、ADV filter。
    """

def find_parameter_stability_region(
    heatmap_df: pd.DataFrame,
    cv_threshold: float = 0.10,  # CV < 10% 视为稳定
) -> dict:
    """
    分析参数稳定区间。
    判断标准（设计方案 1.2.5）：
    1. Flatness：无尖峰（相邻参数性能变化 < 阈值）
    2. Low Sensitivity：CV < cv_threshold
    3. 连续区间：非孤立点
    """
```

---

#### 3.5.6 执行压力测试 `analysis/stress.py`（对应1.2.6）

```python
def run_slippage_stress(
    baseline_results: BacktestResults,
    slippage_levels: list[float],  # [5, 10, 20, 30] bps
    price_panel: dict,
    indicators: dict,
    baseline_params: StrategyParams,
    start: str,
    end: str,
) -> pd.DataFrame:
    """
    非线性滑点压力测试。
    对每个滑点水平重新跑完整回测，记录绩效变化。
    """

def run_latency_stress(
    baseline_params: StrategyParams,
    delay_days: list[int],  # [0, 1, 2] - 执行延迟天数
    price_panel: dict,
    indicators: dict,
    start: str,
    end: str,
) -> pd.DataFrame:
    """
    延迟执行压力测试。
    delay_days=1：信号 t日生成，t+2 开盘执行（而非 t+1）
    delay_days=2：信号 t日生成，t+3 开盘执行
    """

def run_liquidity_stress(
    baseline_params: StrategyParams,
    fill_ratios: list[float],  # [1.0, 0.8, 0.5] - 部分成交比例
    price_panel: dict,
    indicators: dict,
    start: str,
    end: str,
) -> pd.DataFrame:
    """
    部分成交（Partial Fill）压力测试。
    fill_ratio=0.5：只能成交到目标仓位的50%
    """

def generate_execution_sensitivity_curve(
    stress_results: dict,
) -> pd.DataFrame:
    """
    生成设计方案 1.2.6 要求的 Execution Sensitivity Curve 表格：
    
    | Stress Level | CAGR | Sharpe | Max DD | Turnover impact |
    | Base         | ...  | ...    | ...    | 1.0x            |
    | Slippage     | ...  | ...    | ...    | ...             |
    | + Delay      | ...  | ...    | ...    | ...             |
    | Full stress  | ...  | ...    | ...    | ...             |
    """
```

---

#### 3.5.7 Walk-Forward OOS验证 `analysis/walkforward.py`（对应1.2.7）

```python
@dataclass
class WalkForwardWindow:
    is_start: str    # IS区间开始（固定：2004-01-01）
    is_end: str      # IS区间结束
    oos_start: str   # OOS区间开始（= is_end + 1日）
    oos_end: str     # OOS区间结束

# 按设计方案定义4个窗口
WALK_FORWARD_WINDOWS = [
    WalkForwardWindow('2004-01-01', '2021-12-31', '2022-01-01', '2022-12-31'),
    WalkForwardWindow('2004-01-01', '2022-12-31', '2023-01-01', '2023-12-31'),
    WalkForwardWindow('2004-01-01', '2023-12-31', '2024-01-01', '2024-12-31'),
    WalkForwardWindow('2004-01-01', '2024-12-31', '2025-01-01', '2025-12-31'),
]

def run_walk_forward(
    windows: list[WalkForwardWindow],
    price_panel: dict,
    indicators: dict,
    baseline_params: StrategyParams,
) -> dict:
    """
    完整 Walk-Forward OOS 验证流程。

    对每个 WalkForwardWindow：
    Step 1：在 IS 区间内运行全套分析
        - perturbation tests
        - heatmap (IS only)
        - monte carlo
        - execution stress
        → 输出：Final Frozen Parameters（基于IS稳定区间选出的保守参数）

    Step 2：在 OOS 区间用 Final Frozen Parameters 运行回测
        → 输出：OOS CAGR、Sharpe、MaxDD、Turnover、Exposure

    Step 3：拼接4个OOS区间，生成 OOS stitched equity curve

    返回：
    - is_results：每个IS窗口的回测结果和分析
    - oos_results：每个OOS窗口的回测结果
    - oos_stitched_nav：拼接的OOS净值曲线（2022→2025）
    - retention_metrics：CAGR retention、Sharpe retention 等
    """

def select_oos_params(
    is_perturbation: pd.DataFrame,
    is_heatmap: dict,
    is_montecarlo: dict,
    baseline_params: StrategyParams,
) -> StrategyParams:
    """
    按设计方案 1.2.5 的 Final Parameter Selection Rule 选出 OOS 参数。

    Step 1：找出各参数的稳定区间交集
    Step 2：在稳定区间内选取保守内部点（偏保守方向）
    
    禁止：
    - 随机采样
    - 后验选择（no post-selection）
    - 基于OOS反馈调整（no feedback loop）
    """
```

---

#### 3.5.8 市场环境鲁棒性 `analysis/regime.py`（对应1.2.8）

```python
REGIMES = {
    '金融危机':       ('2008-01-01', '2009-12-31'),
    '牛市':           ('2010-01-01', '2019-12-31'),
    'COVID shock':    ('2020-01-01', '2020-12-31'),
    '加息熊市':        ('2022-01-01', '2022-12-31'),
    'recent regime':  ('2023-01-01', '2025-12-31'),
}

def run_regime_analysis(
    daily_returns: pd.Series,
    trade_log: pd.DataFrame,
    regimes: dict = REGIMES,
) -> pd.DataFrame:
    """
    对每个市场环境计算 CAGR、Sharpe、MaxDD。
    
    判断标准：
    - 是否仅在单一 regime 中表现显著（regime dependency）
    - 是否在特定 regime 下出现系统性失效
    """
```

---

### 3.6 报告层（`reports/`）

#### 3.6.1 报告生成 `reports/generator.py`

```python
def generate_baseline_report(
    results: BacktestResults,
    benchmark_ticker: str = 'SPY',
    output_dir: str = 'results/baseline/',
):
    """
    生成完整的 Baseline 报告（设计方案 1.2.1.2 全部 Deliverables）：

    1. QuantStats HTML 报告（净值曲线 vs SPY、月度热力图、回撤图）
    2. 核心绩效指标表格（CSV + 打印到控制台）
    3. 交易明细（trade_log.csv）
    4. Rolling Sharpe（252日滚动）
    5. Rolling Drawdown
    6. Trade-level 统计（PnL分布、持仓时长分布）
    7. Risk decomposition（ETF vs 个股）
    """

def generate_perturbation_report(
    param_name: str,
    results_df: pd.DataFrame,
    output_dir: str,
):
    """
    生成单参数扰动报告（设计方案 1.2.2.2 要求）：

    1. 参数 <-> 绩效表格（打印 + CSV）
    2. Sensitivity CV 值
    3. Stability Region（标注在折线图上）
    4. 结构性结论（自动生成文字描述）
    """
```

---

## 四、Yahoo Finance 数据获取实现

### 4.1 Universe 构建策略

由于 Yahoo Finance 没有 point-in-time 市值和退市股数据，使用以下代理方案：

**方案A（推荐）：S&P 500 历史成分股 + ETF 列表**

```python
# 数据来源：
# 1. S&P 500 历史成分股变动记录（来源：Wikipedia、CRSP代理）
# 2. Russell 1000 当前成分股（代理历史市值 > $20亿）
# 3. 全量 ETF 列表（过滤掉2004年前成立的）

UNIVERSE_SOURCES = {
    'sp500_current': 'https://en.wikipedia.org/wiki/List_of_S%26P_500_companies',
    'etf_list': 'data/static/etf_universe.csv',  # 手工维护的 ETF 清单
}
```

**重要免责声明（在程序中明确标注）**：

Yahoo Finance 原型阶段的结果存在以下已知偏差：
1. **幸存者偏差**：无退市股数据，历史绩效可能虚高 20~50%
2. **市值代理偏差**：使用当前市值代替历史市值，会导致历史上的小市值股（现已成长为大盘股）被错误纳入 universe
3. **ETF inception date 不可靠**：可能纳入了历史上尚未发行的 ETF

这些偏差在切换到商业数据源后会自动消除。原型阶段的主要目的是：
- 验证策略逻辑实现的正确性（信号、止损、仓位计算等）
- 建立回测框架的完整流程
- 发现代码层面的 Bug（非数据层面的偏差）

### 4.2 数据下载脚本 `scripts/01_download_data.py`

```python
"""
使用方法：
    python scripts/01_download_data.py --start 2004-01-01 --end 2025-12-31
    
功能：
1. 从 Yahoo Finance 下载所有 universe 标的的日线数据
2. 计算复权因子，生成 adj_open/adj_high/adj_low
3. 数据清洗（脏数据标记、is_tradable 列）
4. 保存为 Parquet 文件到 data/cache/prices/
5. 同时下载 SGOV（空仓资金代理）和 SPY（benchmark）
"""

def download_ticker(ticker: str, start: str, end: str) -> pd.DataFrame:
    """
    下载单个 ticker 数据，包含：
    1. yf.download() 获取 Open/High/Low/Close/Adj Close/Volume
    2. 计算 adj_factor = Adj Close / Close
    3. 计算 adj_open/adj_high/adj_low
    4. 计算 dollar_vol = adj_close × volume
    5. 数据清洗
    6. 添加 is_tradable 列
    """
```

### 4.3 复权价格计算（关键实现细节）

```
adj_factor[t] = adj_close[t] / close[t]

adj_open[t]  = open[t]  × adj_factor[t]
adj_high[t]  = high[t]  × adj_factor[t]
adj_low[t]   = low[t]   × adj_factor[t]
adj_close[t] = close[t] × adj_factor[t]  (= Yahoo 的 Adj Close)

dollar_vol[t] = adj_close[t] × volume[t]
```

**注意事项**：
- Yahoo Finance 的 `adj_factor` 是"向前调整"（forward-adjusted），即所有历史数据相对于当前价格进行了调整
- 这意味着如果今天下载和明天下载同一只股票，adj_close 的绝对值可能不同（因为有新的股息发放）
- 解决方案：缓存时记录下载日期，若 adj_factor 变化超过阈值则触发全量重新下载

---

## 五、指标预计算流程

回测开始前，对所有 universe 标的批量预计算所有指标，以避免回测循环中的重复计算：

```python
def precompute_indicators(
    price_panel: dict[str, pd.DataFrame],
    params: StrategyParams,
) -> dict[str, dict]:
    """
    预计算所有指标，返回嵌套字典：
    {
        ticker: {
            'atr': pd.Series,           # ATR（Wilder's smoothing）
            'rolling_high': pd.Series,  # rolling max of high (shift=1)
            'breakout_signal': pd.Series,
            'breakout_strength': pd.Series,
            'adv': pd.Series,           # ADV_60（shift=1）
            'log_returns': pd.Series,   # ln(close[t]/close[t-1])
        }
    }
    
    计算完成后，每日的回测循环只需要做：
    indicators[ticker]['atr'][date]  # O(1) 查询
    """
```

**对参数扫描的影响**：
- 当参数改变时，只需要重新计算**受该参数影响的指标**
- 例如：改变 `breakout_window`，只需重新计算 `rolling_high` 和 `breakout_signal`
- `atr` 和 `adv` 不受影响，可以复用缓存

---

## 六、数据源切换方案

### 6.1 切换流程

当购买商业数据后，切换步骤如下（**只需要修改一处**）：

**Step 1**：实现新的 Adapter 类

```python
# data/adapters/polygon.py
class PolygonAdapter(DataSourceAdapter):
    def __init__(self, api_key: str):
        self.api_key = api_key

    def get_ohlcv(self, ticker, start, end) -> pd.DataFrame:
        # 调用 Polygon.io API
        # 返回格式与 YahooFinanceAdapter 完全相同
        # 重点处理：包含退市股，point-in-time 调整，干净的开盘价
        ...

    def get_universe(self, date, min_price, min_market_cap_b, min_adv_m) -> list[str]:
        # Polygon.io 支持按日期的 universe 查询
        # 包含该日期之前上市的所有股票（含已退市）
        ...

    def get_market_cap(self, ticker, date) -> float:
        # Polygon.io 提供 point-in-time 市值
        ...
```

**Step 2**：修改入口文件中的 adapter 实例化（唯一修改点）

```python
# scripts/02_run_baseline.py

# 原来：
adapter = YahooFinanceAdapter()

# 改为：
adapter = PolygonAdapter(api_key=os.environ['POLYGON_API_KEY'])

# 其余代码完全不变
```

**Step 3**：重新下载并缓存数据

```bash
python scripts/01_download_data.py --adapter polygon --start 2004-01-01
```

### 6.2 不同数据源的差异处理

| 差异点 | Yahoo Finance | Polygon.io / Norgate |
|---|---|---|
| 退市股 | 无，手动添加限制说明 | 包含，自动处理 |
| Point-in-time 市值 | 用当前市值代理 | 原生支持 |
| ETF inception date | 无，用静态列表 | 原生支持 |
| 复权方式 | 向前复权（自己计算adj OHLV） | 向后复权或向前复权（直接提供） |
| 数据质量 | 偶发脏数据，需清洗 | 更干净，清洗规则可简化 |

不同数据源的处理差异完全封装在 Adapter 内部，上层代码无感知。

---

## 七、各测试的运行脚本说明

### 7.1 `scripts/02_run_baseline.py`

```bash
python scripts/02_run_baseline.py \
    --start 2004-01-01 \
    --end 2025-12-31 \
    --initial-capital 10000000 \
    --output results/baseline/
```

输出：
- `results/baseline/nav.csv`：每日 NAV
- `results/baseline/trades.csv`：完整交易明细
- `results/baseline/metrics.json`：核心绩效指标
- `results/baseline/report.html`：QuantStats HTML 报告
- `results/baseline/rolling_sharpe.png`：滚动 Sharpe 图
- `results/baseline/pnl_distribution.png`：R倍数分布图

---

### 7.2 `scripts/03_run_perturbation.py`

```bash
python scripts/03_run_perturbation.py \
    --param breakout_window \
    --values 80,90,100,110,120 \
    --start 2004-01-01 \
    --end 2025-12-31 \
    --workers 4 \
    --output results/perturbation/
```

逐个参数运行（按设计方案 1.2.2.1 的参数网格），每次只改一个参数，其他固定在 baseline。

输出：
- `results/perturbation/{param_name}_sensitivity.csv`：参数 vs 绩效表格
- `results/perturbation/{param_name}_sensitivity.png`：折线图
- `results/perturbation/summary.md`：结构性结论（需人工补充）

---

### 7.3 `scripts/05_run_montecarlo.py`

```bash
python scripts/05_run_montecarlo.py \
    --baseline-results results/baseline/nav.csv \
    --n-simulations 1000 \
    --method both  # return_bootstrap + block_bootstrap
    --output results/montecarlo/
```

输出：
- `results/montecarlo/nav_paths.png`：1000条净值曲线（含5%/median/95%）
- `results/montecarlo/drawdown_distribution.png`：最大回撤分布
- `results/montecarlo/drawdown_duration_table.csv`：连续亏损长度分布
- `results/montecarlo/cagr_distribution.png`：CAGR分布
- `results/montecarlo/metrics.json`：关键统计指标

---

### 7.4 `scripts/06_run_heatmap.py`

```bash
# 1D Heatmap
python scripts/06_run_heatmap.py \
    --type 1d \
    --param breakout_window \
    --values 80,100,120,150 \
    --is-end 2021-12-31 \  # 严格限制在IS区间
    --output results/heatmap/

# 2D Heatmap
python scripts/06_run_heatmap.py \
    --type 2d \
    --param1 breakout_window --values1 80,100,120,150 \
    --param2 stop_loss_multiplier --values2 1.5,2.0,2.5,3.0 \
    --metric Sharpe \
    --is-end 2021-12-31 \
    --output results/heatmap/
```

⚠️ `--is-end` 参数强制限制回测只用 IS 数据，无法用 OOS 数据，防止参数污染。

---

### 7.5 `scripts/08_run_walkforward.py`

```bash
python scripts/08_run_walkforward.py \
    --output results/walkforward/
```

自动按设计方案中的4个 IS/OOS 窗口运行，每个 IS 窗口内自动运行：
1. Perturbation（子集参数扫描）
2. Heatmap（IS 数据）
3. Monte Carlo
4. 选出 Final Frozen Parameters
5. 在 OOS 上运行回测

输出：
- `results/walkforward/oos_stitched_nav.csv`：拼接的 OOS 净值曲线（2022→2025）
- `results/walkforward/per_window/`：每个窗口的详细结果
- `results/walkforward/retention_metrics.csv`：IS vs OOS 性能保留率

---

## 八、策略2.0的扩展路径

设计方案确保了从策略1.0到2.0的平滑过渡：

| 模块 | 策略1.0 | 策略2.0 | 修改量 |
|---|---|---|---|
| 数据层 | 完全共用 | 完全共用 | 0 |
| 指标层 | ATR、突破信号、ADV | 增加：横盘收敛、触顶次数、突破强度 | 新增3个函数 |
| Entry 逻辑 | `strategy/v1/entry.py` | `strategy/v2/entry.py`（新文件） | 新文件 |
| Exit 逻辑 | ATR trailing stop（分段） | 结构止损 + 简化trailing | 新文件 |
| 仓位计算 | `strategy/v1/sizing.py` | 完全共用 | 0 |
| 回测引擎 | 完全共用 | 完全共用 | 0 |
| 分析层 | 完全共用 | 完全共用 | 0 |
| 报告层 | 完全共用 | 完全共用 | 0 |

策略2.0只需要：
1. 在 `indicators/` 新增横盘收敛、触顶次数等指标函数
2. 创建 `strategy/v2/entry.py`，继承 `BaseStrategy`，覆盖 `generate_entry_signals`
3. 创建 `strategy/v2/strategy_v2.py`，组装新的入口逻辑

策略2.0的所有测试（perturbation、heatmap、Monte Carlo、Walk-Forward）直接复用现有的 `analysis/` 模块，无需修改。

---

## 九、实施顺序建议

按以下顺序实施，每步完成后有明确的验收标准：

### Phase 0：环境搭建（1-2天）

```bash
pip install yfinance pandas numpy scipy matplotlib seaborn quantstats pyarrow fastparquet
```

验收：能够导入所有依赖，运行一个简单的 yfinance 下载测试。

---

### Phase 1：数据层（3-5天）

实施顺序：
1. `data/adapters/base.py`（接口定义）
2. `data/adapters/yahoo.py`（Yahoo 实现）
3. `data/pipeline.py`（数据清洗和缓存）
4. `data/universe.py`（universe 管理）
5. `scripts/01_download_data.py`（下载脚本）

验收标准：
- 下载 50 个 ticker 的 2004-2025 数据，保存到 Parquet
- 读取 Parquet，检查复权价格（adj_open ≤ adj_high，adj_low ≤ adj_open）
- `is_tradable` 正确标记（停牌日、脏数据日）
- 增量更新有效（只下载缺失的日期范围）

---

### Phase 2：指标计算层（3-4天）

实施顺序：
1. `indicators/atr.py`
2. `indicators/breakout.py`
3. `indicators/adv.py`
4. `indicators/correlation.py`

验收标准：
- 手动验证 ATR（Wilder's smoothing）：取一只股票，对比 TradingView 上的 ATR(20) 值
- 手动验证 rolling_high：取一只股票，确认 `rolling_high[t] = max(high[t-N:t-1])`（不含当日）
- 手动验证 ADV：确认使用 shift(1)，不包含当日成交量

---

### Phase 3：策略逻辑层（5-7天）

实施顺序：
1. `strategy/params.py`（ParamSet）
2. `strategy/v1/entry.py`（开仓信号生成）
3. `strategy/v1/exit.py`（止损/止盈）
4. `strategy/v1/sizing.py`（仓位计算）
5. `strategy/v1/strategy_v1.py`（组装）

验收标准：
- 对单个交易信号手动计算，验证仓位计算结果（raw → capped → correlation → heat）
- 对单个持仓手动验证 trail_stop 更新（分段公式是否正确）
- 验证优先级规则（stop_loss > trailing_stop）

---

### Phase 4：回测引擎层（7-10天）

实施顺序：
1. `backtest/position.py`（持仓状态机）
2. `backtest/portfolio.py`（组合管理）
3. `backtest/execution.py`（交易执行）
4. `backtest/results.py`（结果容器）
5. `backtest/engine.py`（主引擎）

验收标准：
- 单测试（5只股票，1年）：手动验证每笔交易的入场价、止损价、出场价是否正确
- 验证 look-ahead bias：确认 t 日信号用 t+1 开盘价执行
- 验证 heat 更新顺序（每笔开仓后立即更新）
- 验证 SGOV 计入 NAV

---

### Phase 5：Baseline 回测运行（2-3天）

1. 用全量 universe，2004-2025，Baseline anchor 参数跑完整回测
2. 生成完整的 Baseline 报告
3. 对照 SPY 检查净值曲线是否合理
4. 检查 trade_log 是否有明显异常

---

### Phase 6：分析层（10-15天）

按设计方案顺序：

**Week 1**：
1. `analysis/metrics.py`（核心指标）
2. `reports/generator.py`（基础报告）
3. `scripts/02_run_baseline.py`

**Week 2**：
4. `analysis/perturbation.py`（扰动测试）
5. `scripts/03_run_perturbation.py`
6. `analysis/diagnostics.py`（交易诊断）
7. `scripts/04_run_diagnostics.py`

**Week 3**：
8. `analysis/montecarlo.py`（蒙特卡洛）
9. `scripts/05_run_montecarlo.py`
10. `analysis/heatmap.py`（参数热力图）
11. `scripts/06_run_heatmap.py`

**Week 4**：
12. `analysis/stress.py`（执行压力测试）
13. `scripts/07_run_stress.py`
14. `analysis/walkforward.py`（Walk-Forward）
15. `scripts/08_run_walkforward.py`
16. `analysis/regime.py`（Regime 分析）
17. `scripts/09_run_regime.py`

---

## 十、关键设计决策说明

### 10.1 为什么选择自定义引擎而非 Backtrader

Backtrader 对单标的事件驱动策略非常适合，但本策略的特点是：

1. **多标的同步状态**：每日需要在全市场扫描信号，跨标的计算相关性，管理数十个持仓的状态机
2. **多信号排序处理**：同一天多个开仓信号按 breakout_strength 排序，逐个处理并立即更新组合状态
3. **热力图/扰动测试**：需要运行数百次完整回测，Backtrader 的 `Cerebro` 初始化开销较大
4. **调试需要**：策略逻辑透明度很重要，自定义引擎的状态可以随时检查和打印

自定义引擎的代价：需要自己实现事件循环和状态管理，约需多 5-7 天的开发时间。
收益：调试效率更高，参数扫描速度更快（多进程更容易），策略逻辑更清晰。

### 10.2 为什么预计算所有指标

不在每日循环中实时计算指标，而是在回测开始前批量预计算，原因：

1. **速度**：ATR 的 Wilder's smoothing 是顺序计算，向量化一次计算比循环中逐日计算快 100 倍
2. **参数扫描**：改变 `breakout_window` 时，只需重新计算 `rolling_high`，ATR 不需要重新计算
3. **正确性**：预计算可以避免"每日回测循环中使用了未来数据"的 bug（look-ahead bias）

### 10.3 SGOV 的处理

设计方案要求空仓资金投入 SGOV 并计入 NAV。实现方式：

- SGOV 的日收益率单独下载和缓存
- 每日计算"可用现金"（总现金 - 明日待执行订单所需资金）
- 可用现金 × SGOV 当日收益率 = SGOV 当日贡献的利息，加入 NAV
- SGOV 不参与开仓信号计算和相关性计算

---

## 十一、Yahoo Finance 已知问题的规避策略

| 已知问题 | 规避方法 | 对结果的影响 |
|---|---|---|
| 无退市股 | 明确标注偏差，用 universe 说明文件记录 | CAGR 虚高 20-50%，Sharpe 虚高 |
| 市值非point-in-time | 用当前市值代理（偏向大市值） | 历史上的小盘股被错误过滤，universe 偏大盘 |
| ETF inception date 不可靠 | 手动维护 ETF 清单（含成立日期） | 可能纳入历史上不存在的 ETF |
| 开盘价偶发脏数据 | 清洗规则（open > high 时替换） | 少量异常交易，影响有限 |
| 复权因子变化 | 缓存时记录下载时间，超过30天触发重新下载检查 | 价格序列不一致，可能产生虚假突破信号 |

---

## 十二、交付标准（每个阶段的结束条件）

### Baseline 完成标准（1.2.1）
- [ ] 全量 universe，2004-2025，完整回测运行无报错
- [ ] trade_log 中每笔交易的数据字段完整（入场、出场、止损价、ATR、R倍数）
- [ ] QuantStats HTML 报告生成成功，净值曲线视觉检查通过
- [ ] CAGR、Sharpe、MaxDD 数值与手工抽查结果一致

### 完整测试套件完成标准（1.2.2 ~ 1.2.8）
- [ ] 扰动测试对所有 15 个参数运行完成，输出 CSV 和折线图
- [ ] 蒙特卡洛 1000 条路径运行完成（return bootstrap + block bootstrap）
- [ ] 参数热力图（5个1D + 2个2D）在严格 IS 数据上生成
- [ ] 执行压力测试（slippage × latency × partial fill）完成
- [ ] Walk-Forward 4个窗口全部完成，OOS stitched 净值曲线生成
- [ ] Regime 分析完成，每个 regime 的 CAGR/Sharpe/MaxDD 输出

---

*本计划书版本：v1.0，对应设计方案 Session 1 和 Session 1.2*
*策略2.0的实现计划将在策略1.0完整测试验证后另行编写*
