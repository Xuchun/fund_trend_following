"""参数热力图分析 — 实现计划（尚未运行计算）"""

import sys
from pathlib import Path

_root = Path(__file__).resolve().parents[2]
if str(_root) not in sys.path:
    sys.path.insert(0, str(_root))

import streamlit as st
from website.shared import get_results
from website.components.strategy_badge import render_page_header

res  = get_results()
meta = res.meta
p    = meta.params_anchor

render_page_header("参数热力图分析", meta)
st.caption(f"{meta.display_name} · 回测期间：{meta.backtest_start} → {meta.backtest_end}")
st.markdown("---")


# ── 0. 重要说明 ─────────────────────────────────────────────────────────────────
st.info(
    "📌 **关于数据范围：** 热力图分析与参数扰动测试使用**相同的完整历史数据（2004–2026）**，保持一致性。"
    "策略参数在运行回测前已根据业界经验事先确定（N=200、止损 2×ATR 等），并非通过观察结果后反向挑选，"
    "因此不存在数据泄漏。两种分析的目的均为理解参数景观，而非为新策略版本选优。"
    "若未来需要为 Strategy 2.0 挑选参数，届时才须严格限制在 IS 截止日之前的数据。"
)

st.markdown("""
### 什么是参数热力图分析？

参数敏感性分析（Perturbation Tests）已逐参数单独测试；
热力图分析在此基础上进一步探索**两个参数联合变化时的稳定区间**：

| 分析类型 | 维度 | 目的 |
|---------|------|------|
| Perturbation Tests（已完成） | 1D，逐参数 | 验证单参数鲁棒性（使用全期数据） |
| **1D Heatmap（本页）** | 1D，可视化 | 以条形图呈现参数景观平坦性（全期数据） |
| **2D Heatmap（本页）** | 2D，双参数网格 | 识别两参数联合稳定区间（全期数据） |
| Slice Tests（本页） | 1D，固定锚点 | 替代高维网格，控制计算量（全期数据） |

参数稳定区间（Stability Region）的判断标准（设计方案 1.2.5）：
- **Flatness**：区间内无单一尖峰（Sharp Peak）
- **Low Sensitivity**：CV = std(Sharpe) / mean(Sharpe) < 0.10
- **连续区间**：稳定点必须相互毗连，非孤立点
""")

st.markdown("---")


# ── 数据范围说明与 Walk-Forward 框架 ──────────────────────────────────────────
st.subheader("📋 数据范围说明与 Walk-Forward 验证框架")

st.markdown(f"""
#### 为什么热力图和扰动测试都使用完整历史数据（{meta.backtest_start} → {meta.backtest_end}）？

本策略所有参数（N=200、止损 2×ATR 等）在运行任何回测**之前**，已根据业界经验和趋势跟踪机制原理事先确定，
**没有经过"观察 OOS 结果 → 调参 → 再测试"的循环**。因此：

- 热力图和扰动测试的目的是**验证鲁棒性**（理解参数景观），而非**挑选参数**。
- 由于不存在 OOS 数据泄漏，使用完整数据在方法论上可接受。
- 两种分析使用相同的数据范围，保持一致性。

#### 两种分析的对比

| | Perturbation Tests（已完成）| Heatmap（本页） |
|-|--------------------------|----------------|
| 数据范围 | {meta.backtest_start} → {meta.backtest_end}（全期数据）| {meta.backtest_start} → {meta.backtest_end}（全期数据）|
| 是否存在数据泄漏 | 否（参数未基于 OOS 挑选）| 否（目的为验证鲁棒性，非选参）|
| 分析维度 | 1D，逐参数 | 1D 可视化 + 2D 双参数网格 |
| 用途 | 验证单参数鲁棒性 | 验证双参数联合稳定性 |
| 结果能否用于 Strategy 2.0 选参 | **不应直接用于选参** | **不应直接用于选参** |

**Strategy 2.0 参数选择时**，届时须严格限制在 IS 截止日（2021-12-31）之前的数据，
并经 OOS 验证，才能得出无偏的参数稳定区间。

#### 热力图网格扫描步骤

以 N × 止损乘数的 2D 网格为例（共 28 次回测）：

| N \\\\ 止损乘数 | 1.5× | 2.0×（基准）| 2.5× | 3.0× |
|--------------|------|------------|------|------|
| N=150 | Sharpe=? | Sharpe=? | Sharpe=? | Sharpe=? |
| N=200（基准）| Sharpe=? | **Sharpe=0.47** | Sharpe=? | Sharpe=? |
| N=300 | Sharpe=? | Sharpe=? | Sharpe=? | Sharpe=? |

（实际数字在回测完成后填入）

从热力图中识别稳定区间（Sharpe ≥ 90% 峰值的连续区域）示意：

```
N ∈ [170, 250]，止损乘数 ∈ [1.5, 2.5]  →  稳定区间（示意）
```

稳定区间须在**时间分片**（前半段 2004–2015、后半段 2016–2026）中均成立，才算通过。
""")

st.markdown("---")


# ── 1. 1D Heatmap 计划 ─────────────────────────────────────────────────────────
st.subheader("📊 计划一：1D Heatmap（单参数景观可视化）")
st.markdown(f"""
对以下 **5 个核心参数**生成条形图 + 折线图，可视化参数景观（Parameter Landscape）。
参数列表与测试范围直接来自设计方案 1.2.5"1D Heatmap（5个以内）"。

| 参数 | 当前基准值 | 测试范围 | 设计方案要求 | 数据来源 |
|------|-----------|---------|------------|---------|
| 突破窗口 N（breakout_window） | **{p['breakout_window']}日** | 150/170/200/230/250/270/300 | ✅ 必须 | 已有 JSON |
| ATR 周期（atr_period） | **{p.get('atr_period', 20)}日** | 10/15/20/30 | ✅ 必须 | 需新增回测 |
| ATR 止损乘数（stop_loss_multiplier） | **{p['stop_loss_multiplier']:.1f}×** | 1.5/2.0/2.5/3.0 | ✅ 必须 | 已有 JSON |
| 移动止盈乘数（trail_multiplier_r1） | **{p['trail_multiplier_r1']:.0f}×ATR** | 2.0/2.5/3.0/3.5/4.0 | ✅ 必须 | 已有 JSON |
| 最小止损距离（min_stop_distance_pct） | **{p.get('min_stop_distance_pct', 0.005)*100:.1f}%** | 0.3%/0.5%/1.0% | 可选 | 需新增回测 |

**每张 1D 图展示：**
- X 轴：参数取值
- Y 轴（左）：Sharpe（主指标）
- Y 轴（右）：CAGR（次指标）
- 基准值标注（竖线）
- 稳定区间高亮（Sharpe ≥ 90% 峰值的连续区间）
- CV 值标注（Flatness 评估）

**判读方式：**
- ✅ 高原形态（Plateau）：参数大范围内 Sharpe 近似水平 → 鲁棒
- ⚠️ 单调递增/递减：可能反映机制优势，也可能是过拟合，需结合 OOS 判断
- 🔴 尖峰形态（Sharp Peak）：仅特定值表现最优 → 过拟合风险高
""")

st.info(
    "💡 **计算量：** 突破窗口 N、ATR 止损乘数、移动止盈乘数直接复用已有 Perturbation JSON，无需额外回测。"
    "ATR 周期（4 个值）和最小止损距离（3 个值）需新增共 7 次全宇宙回测，预计约 1 小时。"
)

st.markdown("---")


# ── 2. 2D Heatmap 计划 ─────────────────────────────────────────────────────────
st.subheader("🗺️ 计划二：2D Heatmap（双参数联合网格扫描）")
st.markdown(f"""
对两对参数组合进行完整 **2D 网格扫描**，逐一运行历史回测并记录 Sharpe/CAGR/MaxDD。
这是本页唯一需要重新运行回测的部分。

#### 必须做：N × 止损乘数

| 参数 | 网格值 | 网格点数 |
|------|--------|---------|
| 突破窗口 N | 150 / 170 / 200 / 230 / 250 / 270 / 300 | 7 |
| ATR 止损乘数 | 1.5 / 2.0 / 2.5 / 3.0 | 4 |
| **网格总点数** | | **7 × 4 = 28 次回测** |

#### 可选做：N × 移动止盈乘数

| 参数 | 网格值 | 网格点数 |
|------|--------|---------|
| 突破窗口 N | 150 / 200 / 250 / 300 | 4（精简版） |
| trail_multiplier_r1 | 2.0 / 2.5 / 3.0 / 3.5 / 4.0 | 5 |
| **网格总点数** | | **4 × 5 = 20 次回测** |

**每张 2D 图展示：**
- 行：参数 A 的取值（如 N）
- 列：参数 B 的取值（如 止损乘数）
- 单元格颜色：Sharpe 值（绿色高 → 红色低）
- 单元格数字：Sharpe（主）/ CAGR（副）
- 基准组合标注（★）
- 稳定区间边框高亮（Sharpe ≥ 90% 最优值）

**判读方式：**
- ✅ 大片绿色高原：两参数联合变化时，大范围内均表现良好 → 高鲁棒性
- ⚠️ 对角线绿色（仅某方向稳定）：参数存在补偿效应，需谨慎
- 🔴 孤立绿色点：仅单一参数组合表现最优 → 过拟合

**时间分片验证（Sub-Period）：**
除全样本（2004–2026）外，还将分别生成：
- 前半段（2004–2015）：覆盖金融危机、低利率周期
- 后半段（2016–2026）：覆盖低波动牛市、疫情冲击、加息周期

稳定区间必须在**两个子区间中均成立**，才算通过。
""")

st.info(
    f"💡 **计算量：** 2D 网格总计最多 28 + 20 = **48 次全宇宙回测**。"
    f"参照 Perturbation 测试每次约 6 分钟，预计总计算时间约 **4–5 小时**（单进程）。"
    f"基准组合（N={p['breakout_window']}，止损 {p['stop_loss_multiplier']:.0f}×）已有结果，可跳过。"
)

st.markdown("---")


# ── 3. Slice Tests 计划 ────────────────────────────────────────────────────────
st.subheader("🔪 计划三：Slice Tests（固定锚点的高维替代方案）")
st.markdown(f"""
对无法做完整 2D 扫描的参数，固定 N={p['breakout_window']}、止损 {p['stop_loss_multiplier']:.0f}×ATR（Baseline anchor），
分别扰动单参数，以 1D 折线图呈现。

这部分的数据**已在"参数敏感性分析"中全部计算完成**，直接复用。

| 参数 | 测试值 | 数据来源 |
|------|--------|---------|
| 相关性阈值 | 0.5/0.6/0.7/0.8/0.9 | ✅ 已有 JSON |
| 每笔风险比例 | 0.5%/1.0%/1.5%/2.0% | ✅ 已有 JSON |
| 最低市值过滤 | $2B/$3B/$4B | ✅ 已有 JSON |
| ADV 流动性过滤 | $10M/$20M/$30M | ✅ 已有 JSON |
| 成交量确认乘数 | 1.0/1.2/1.5/1.7/2.0 | ✅ 已有 JSON |
| 滑点 | 5/8/10/12/15 bps | ✅ 已有 JSON |
| 佣金 | 1/3/5 bps | ✅ 已有 JSON |
""")

st.info("💡 **计算量：** Slice Tests 全部复用已有数据，无需额外回测。")

st.markdown("---")


# ── 4. 最终参数选择规则 ────────────────────────────────────────────────────────
st.subheader("🎯 计划四：最终参数选择规则（OOS 前的最后一步）")
st.markdown("""
依据设计方案 1.2.5 的"Final Parameter Selection Rule for OOS"，
在完成所有热力图分析后，将生成一张综合稳定区间交集表：

**Step 1：筛选三项检验的交集稳定区间**

| 参数 | Perturbation 稳定区间 | Heatmap 稳定区间 | Monte Carlo 通过？ | 交集区间 |
|------|-------------------|---------------|----------------|---------|
| 突破窗口 N | 待计算 | 待计算 | 待计算 | 待定 |
| ATR 止损乘数 | 待计算 | 待计算 | 待计算 | 待定 |
| trail_multiplier_r1 | 待计算 | 待计算 | 待计算 | 待定 |
| 相关性阈值 | 待计算 | 待计算 | 待计算 | 待定 |

**Step 2：在交集区间内选取稳健内部点（保守偏向）**

选取原则（设计方案明确规定）：
- 位于稳定区间**中部区域**，不在局部峰值
- 参数方向**偏保守**（如止损乘数偏大一点，减少过早止损）
- 禁止从稳定区间随机采样
- 禁止多参数组合后挑选（Post-selection bias）
- 禁止根据 OOS 表现回调参数

❌ **明确禁止：**
- 选取 Heatmap 中 Sharpe 最高的参数组合作为实盘参数
- 利用 OOS 结果反向调整参数后再"验证"
""")

st.markdown("---")


# ── 5. 实施路线图 ──────────────────────────────────────────────────────────────
st.subheader("🗓️ 实施路线图")
st.markdown(f"""
| 步骤 | 内容 | 计算量 | 状态 |
|------|------|--------|------|
| 1 | 1D Heatmap（复用 Perturbation 数据） | 无额外回测 | 🔜 待实现 |
| 2 | Slice Tests（复用 Perturbation 数据） | 无额外回测 | 🔜 待实现 |
| 3 | 2D Heatmap：N × 止损乘数（28 次回测） | 约 3 小时 | 🔜 待实现 |
| 4 | 2D Heatmap：N × 移动止盈乘数（20 次回测，可选） | 约 2 小时 | 🔜 待实现 |
| 5 | 子区间验证（前/后半段） | 与步骤 3/4 同时跑 | 🔜 待实现 |
| 6 | 最终参数选择规则综合表 | 无额外回测 | 🔜 待实现 |

**优先级建议：** 步骤 1 → 2 → 3 → 6（步骤 4/5 为可选增强项）
步骤 1 和 2 可在"参数敏感性分析"后台任务完成后立即实现（无需等待），步骤 3 是计算瓶颈。

**数据一致性：** 所有回测使用相同的数据截止日期（{meta.backtest_end}）和 Baseline anchor 参数作为固定对照。
""")

st.markdown("---")
st.info(
    "📌 **当前状态：** 本页为实施计划，尚未运行热力图计算。"
    "确认计划无误后，将依次实现各步骤并在此页面展示结果。"
)
