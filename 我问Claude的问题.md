根据《趋势跟踪策略1.0-设计方案.md》里面的session 1和session 2，请写一个《趋势跟踪策略历史回测程序实现计划书.md》。这个计划书里面，详细写清楚如果使用yahoo finance数据，你如何程序实现“趋势跟踪策略1.0”和sesseion 1.2里面的“策略 1.0 历史回测”。你写的程序要保证很多模块可以再用，因为你写完趋势跟踪策略1.0的历史回测和各种测试后，你需要继续改动这个代码去实现趋势跟踪策略2.0和对应的各种测试。至于数据，目前使用yahoo finance，当时当代码都调通之后，我会购买质量更好的数据，所以你的代码要将来很容易的从处理yahoo finance的数据换成处理其他数据来源。

请基于《趋势跟踪策略历史回测程序实现计划书.md》里面的“## 九、实施顺序建议”完成“Phase 5：Baseline 回测运行"

在《趋势跟踪策略历史回测程序实现计划书.md》里面的“九、实施顺序建议”的”Phase 6：分析层“之后，加一个"Phase 7: 趋势跟踪策略历史回测网站"。在这个网站里要写清楚：回测的”趋势跟踪策略“的详细描述和根本逻辑，回测使用的数据，回测使用了哪些标的（具体哪些股票和ETF，给出ticker），写清楚回测一共包括了多少只股票和多少只ETF，回测的历史时间区间，回测的方法，回测做了哪些参数分析，回测的结果，回测得到的各种结果的分析和评估，回测的局限性在哪里，最终回测后得到的推荐实盘的策略和对应参数，以及下一步该做什么。这个网站要专业级别，因为我会把网站上面显示的趋势跟踪策略回测结果给专业的portfolio manager看，他可是在管理几千万美元投资资产的20多年的专家。

请包含“data”文件夹里面的《ETFs.csv》里面的所有ETF到你的历史回测里面。请对应更新网站

请用增加了SPY 200日均线来做过滤器的策略1.0来重新跑历史回测，历史回测要包含“data”文件夹里面的《ETFs.csv》里面的所有ETF到你的历史回测里面。请对应更新网站。

程序实现“突破强度过滤（Breakout Strength Filter）”：不仅要求"收盘价 > 100 日高点"，还要求突破幅度 > X%（如 1%–2%）。程序实现完，做完整的历史回测，用baseline，然后把结果更新到网站，还要更新网站对于策略的描述

把”策略逻辑“网页里面的”3. 成交量确认（Volume Confirmation）“改成”3. 成交量过滤“

把“策略逻辑”网页里面的“9. 市场环境过滤”块放到“1. 开仓前过滤条件”块之后，“2. 入场条件”块之前，其他号码对应修改。

请仔细核查你的策略回测的每一个代码文件，看看它们跟《趋势跟踪策略1.0-设计方案.md》里面的session 1（1. 趋势跟踪策略1.0），是否100%的一模一样。如果有不一致，你先写给我，我要评估一下再决定是否修改。

请基于《趋势跟踪策略历史回测程序实现计划书.md》里面的“九、实施顺序建议”完成“Phase 6：分析层"的“Week 4”任务。

参数敏感性分析网页，对于热度上限和最低市值过滤，不同的参数，CAGR和MaxDD几乎一样，这合理吗？请仔细核对你的代码，和做的参数敏感性分析，我感觉不同参数值，CAGR和MaxDD应该有一定的差异的

为啥“Baseline参数回测结果”里面显示的CAGR（年化复合回报）是 +8.4%，而“参数敏感性分析”网页的“ATR 止损乘数”下面当参数值**2.0×（基准）**的时候，CAGR+8.08%。为啥这两个CAGR不一样？

在“Baseline参数回测结果”网页的“净值曲线 vs SPY”上面，网页上部的指标模块的下面，写清楚Baseline的所有参数的具体值，需要仔细核对这些值跟你的代码上面的值，还有设计方案里面的值，完全100%一致

请用最新的历史数据，最新的代码，重新跑一下网页“参数敏感性分析”里面的参数敏感性分析，根据“分析框架”的表格里面提供的所有参数，和每个参数的测试范围，全部仔细测试一下每个参数的敏感性分析。然后把参数敏感性分析的结果更新到网站上面，并且给出对于每个参数的敏感性分析的“评估”和最终所有参数敏感性分析的“评估”。你写”评估“的时候，可以参考设计方案里面的”**1.2.2.2 Deliverables**“和”**1.2.2.3 结构性结论“。

请在””下面生成一个新的网页，叫做“参数热力图分析”，然后根据设计方案里面的“1.2.5 Parameter heatmap”，在这个新网页上，写一下你打算如何做这个参数热力图分析，但是先不要程序实现，我要看看确保无误之后，你再程序实现。

------------ 以下内容还没有提交给Claude -------------

请仔细核对https://fundtrendfollowing-bsauqpmdcabcxxszw2q7gd.streamlit.app/的“策略信息”下面的三个网页的描述，是否跟《趋势跟踪策略1.0-设计方案.md》里面的session 1（1. 趋势跟踪策略1.0），100%的一模一样，这个非常重要。你先不要修改网页或者《趋势跟踪策略1.0-设计方案.md》，如果有不一致，你先写给我，我要评估一下再决定是否修改。

请仔细核对你的每一个代码文件，是否跟https://fundtrendfollowing-bsauqpmdcabcxxszw2q7gd.streamlit.app/里面的“策略信息”下面的三个网页的描述完全100%一致，如果有不一致，你先写给我，我要评估一下再决定是否修改。

请仔细核对你的每一个代码文件，是否跟《趋势跟踪策略1.0-设计方案.md》里面的”1. 趋势跟踪策略1.0“描述完全100%一致，如果有不一致，你先写给我，我要评估一下再决定是否修改。

请重新用最新的数据跑策略1.0的历史回测，包括baseline测试，参数敏感性分析，蒙特卡洛风险（使用 Baseline anchor 锚点参数），Walk-Forward 验证市场环境分析。也就是说，对于网页上面当前得到的每一个数据和图表，请重新跑程序生成。然后对应更新网页，给出比较的小结。